> Delta review of the review repairs (98911df..57540a3) for v6.13.0, by an independent subagent. P3-2 was fixed (completed, un-negated applications only), P3-1/P3-3 are documented as known limits in scripts/green-stamp-reporter.mjs, P3-4/P3-5 and FALSE-1..3/5 corrected in the files; FALSE-4/6 (commit bodies) are itemised in the next commit body.

# Delta review 98911df..57540a3 (65a77bb, e7c064e, 46a6565, 57540a3)

Anchor OK: `git log --oneline 98911df..57540a3` lists exactly the 4 commits. Everything below was run in a
private `git archive 57540a3` tree (/tmp/delta-lens-*), a private fixture repo, and read-only transcript scans
(2026-09-25 21:43Z to 2026-09-26 05:15Z). The primary repo was never written to (`git status` clean).

Counts: P1 0 · P2 0 · P3 5 · FALSE 6 (plus 3 leftover copies of withdrawn claims, listed under P3-5)

## Baseline evidence (the delta is green)
- Full `npm test` on the 57540a3 tree: `Test Files 431 passed (431); Tests 6695 passed | 1 skipped (6696)`, exit 0.
  The first two runs failed only on `suite-touches-no-repo-files` "…the premise", because install.mjs:1158-1162 detects dogfood by
  `remote.origin.url` and my archive had no remote. Adding the remote fixed it, so this is an environment artifact.
- That full run WROTE the stamp (`.git/mem-lite-green-stamp.json`), and `node scripts/green-stamp.mjs check` answered
  "full suite passed on this exact tree …" with exit 0. So on vitest 5.0.0 a normal full run is not refused:
  `project` resolves to `[]` (vitest dist/chunks/index.B89dZ0-N.js:14312), `cliExclude` is appended only when set (:14479),
  and `dir` is undefined unless `--dir` is passed.
- `npx eslint` on all 9 changed code/test files: exit 0. `npm run format:check`: exit 0.
- Mutation re-run (copy, mutate, run, restore, cmp OK):
  - drop the `cliExclude` line: 2 red (the unit case and the real-vitest case);
  - drop `clearStamp(this.cwd)`: 1 red ("a failing full run on the stamped tree removes the stamp");
  - drop the `--dir` line, then run in a fixture with the default `include`: `vitest run --dir t/sub` stamps a 1-of-3 run.
    So the `--dir` refusal is load-bearing, and the unit row in tests/green-stamp.test.mjs:113 does kill that mutant.

## Flag matrix: real reporter, fixture repo, 3 test files, vitest 5.0.0
| invocation | files run | stamp |
|---|---|---|
| (none) | 3/3 | STAMP |
| `--exclude t/b.test.mjs` | 2/3 | none |
| `--dir t/sub` (default include) | 1/3 | none |
| positional `t/sub` | 1/3 | none (the ranIds check) |
| `--root <abs sub> --config <abs cfg>` (relative reporter path, as in the repo's config) | startup error: reporter fails to load | none |
| `--root <abs sub> --config <abs cfg>` (absolute reporter path) | 1/3 | **STAMP** (not reachable with the repo's config) |
| `--config alt.config.mjs` (narrower include, registers the reporter) | 1/3 | **STAMP**, see P3-3 |
| `--project x` | error, exit 1 | none |
| `--tagsFilter foo` (no tags defined) | error, exit 1 | none, and the stamp is cleared |
| `--typecheck`, `--passWithNoTests`, `--retry 3`, `--bail 1`, `-u`, `--environment node`, `--sequence.shuffle`, all 3 files listed positionally | 3/3 | STAMP |

`--retry N` and `--testTimeout` loosen what counts as a pass without narrowing the population. They still stamp. That is outside
this delta's claims and is noted only.

## P3

**P3-1. A red run with `--reporter=…` leaves the old green stamp in place. The P3-1 repair covers only runs that load the reporter.**
scripts/green-stamp-reporter.mjs:51-60. A CLI `--reporter` replaces the configured reporters, so `clearStamp` never runs.
Fixture: green full run (stamp written), then the same tree with `GS_FAIL=1 vitest run --reporter=dot` exits 1. The stamp survives,
and `green-stamp.mjs check` → "full suite passed on this exact tree", exit 0, so pre-commit would reuse it.
Frequency: 84 of 557 unfiltered `npx vitest run` / `npm test` invocations in this project's transcripts pass `--reporter`
(for example `--reporter=dot`).
The commit body of 65a77bb says "Any failed run (or unhandled error) now deletes the stamp". That sentence is FALSE; see FALSE-6.
The file header (:8-9) calls the `--reporter` bypass "the safe direction". That holds for writing a stamp, but not for clearing one.

**P3-2. isDismissalAt credits NEGATED applications. The new rule reads "does not apply" as "applied".**
lib/citation-tracker.mjs:144-150. `APPLIED_IN_CLAUSE = /\bappl(?:y|ies|ied)\b|\badopted\b|采纳|已按|按它|已应用|用上了/i` has no negation guard.
Probe results (old → new):
- `#12 n/a — does not apply here`: DISMISS → credit
- `#12 n/a: doesn't apply to this read-only path`: DISMISS → credit
- `#12 irrelevant; did not apply`: DISMISS → credit
- `#12 不适用，不采纳` / `#12 不适用，未采纳` / `#12 n/a（不适用：本次未采纳）`: DISMISS → credit
- `#12 n/a, not applied here`: DISMISS → credit
- `#12 与本次无关，没有按它改`: DISMISS → credit

Scope reasons also flip, which is the shape the adopt doc's `'#NN n/a — <理由>'` invites:
- `#12 n/a — it applies to the server path, not this file`
- `#12 n/a — only applies on Windows`
- `#12 n/a (applies to MCP only)`
- `#12 does not apply: …, which I applied elsewhere as #13`

All of these go DISMISS → credit.

Measured impact today is zero. Over 215 top-level transcripts, 1,881 mentions, 328 dismissals in both arms, and 0 removed / 0 added
by name set. So this is a latent false negative in the direction the code calls cheap. It still restores exactly the B2 bug for the
most natural dismissal wording.
The comment at :139-143 ("an application stated later in the SAME clause … means the lesson WAS used") is false for these shapes.

**P3-3. `--config <other file>` that registers the reporter certifies a narrower population.**
Fixture `alt.config.mjs` with `include: ['t/a.test.mjs']` and the reporter: 1/3 files ran and the stamp was written.
`globTestSpecifications()` resolves against the alt config's include, and the stamp key covers the whole repo.
There is no such config in the repo today (only vitest.config.mjs). A `mergeConfig(base, {test:{include:[…]}})` scratch config would trigger it.

**P3-4. The Key Events count leaves its population out.** "50 injections, 250 rows" reproduces only after excluding the analysis session.
My parse of the `hook_additional_context` attachments carrying `### Key Events` in -home-ai-dev-claude-mem-lite/*.jsonl found 52 unique
injections (52 unique uuids) and 260 rows (5 each). The last one was at 2026-09-25T20:38:05Z, so the count was also 52 at 21:40Z.
c3c64219 (this analysis session) holds 2 of them (19:55Z, 20:38Z). The claims review (claims-review.md:19) says "excluding the
analysis session". None of the shipped copies say so:
- CHANGELOG.md:20
- lib/events-injection.mjs:97
- tests/e2e.test.mjs:1053
- the audit doc at :19, :207 and :269

All of them state "this project's main sessions", and c3c64219 is one. This breaks doctrine rule 3.

**P3-5. Copies of withdrawn claims that are still live at 57540a3** (`grep -rnaF`, no file-type filter):
- tests/citation-tracker.test.mjs:134: "(313 of 1877 `#NN` mentions were dismissals, 2026-09-25)". The number was withdrawn in e7c064e,
  and that commit edited this same file 35 lines below.
- docs/audits/20260925-200912-session-history-analysis.md:18: the §0 heading "Key Events … 注入量大、**从未被读取**".
- same file :204: the §4.4 heading "注入多、**从未被读**". The bullet under it (:206) now retracts exactly this inference.
- Pre-existing and not a delta line: §4.4.1 :225 says the shapes are "按出现频率排列". The list reads 6,3,3,1,4,6, which is the ordering
  the withdrawn "most common failure" claim was read from.

No live copy remains of "662" as an injection count, "never followed up", "从未被跟进", "opened by id", "dominant", or "tested bytes are
the committed bytes". The remaining hits are the claims or defect review quoting them, or unrelated numbers.

## FALSE

**FALSE-1. `activity get` does not exist. The subcommand is `activity show`.**
Written at:
- lib/events-injection.mjs:100 ("only `activity get` bumps it")
- the audit doc :206 ("只有 `activity get` 调它")
- claims-review.md:29
- the 46a6565 body

Evidence:
- cli/activity.mjs:128 `if (sub === 'show')` → :134 `getEvent(db, id)`
- `node cli.mjs activity get 1` → "[mem] Unknown activity subcommand: get"
- README.md:127 lists `activity save|search|recent|show`

The mechanism is TRUE: lib/activity.mjs:101 is the only accessed_count writer, and lib/get-core.mjs:124 fetchEventDetail
(server.mjs:961, mem-cli.mjs:869) has no UPDATE.

**FALSE-2. "1,884 `#NN` mentions" does not reproduce.** Written at CHANGELOG.md:30, lib/citation-tracker.mjs:110, audit :336 and the e7c064e body.
I used the shipped `citationIdRe()` plus `isDismissalAt` over /home/ai/.claude/projects/*/*.jsonl, assistant `text` blocks only.
- Cutoff 2026-09-25T21:40Z: **1,881** mentions, 328 dismissals.
- No cutoff (215 files): 1,881 / 328.
- The claims review itself says "328 of 1881 now" (claims-review.md:37).
- Sidechain entries in top-level files: 0. String-content assistant messages: 0. Duplicate uuids: 0.

The 328 is TRUE. The 1,884 is not reproducible under the stated caliber.

**FALSE-3. The audit §8 row says the cite-back hint "只出现过 1 次". The CHANGELOG says "no instance of this hint appears".** They contradict each other; the CHANGELOG is right.
The audit line is docs/audits/20260925-200912-session-history-analysis.md:340; the CHANGELOG line is CHANGELOG.md:53-54.
Across every top-level transcript on the machine, `Cite-back:` or `prior lesson(s) this session` appears in 2 lines:
- 567e5e92 (2026-09-08), an `edited_text_file` attachment showing lib/cite-back-hint.mjs source;
- c3c64219 (20:47Z), a tool_result showing the same source.

The rendered hint appears 0 times.

**FALSE-4. e7c064e body: "the new rule changes only the constructed shapes above".** This is true of the corpus (0/0 name-set diff,
reproduced) and false of the rule, which flips every shape in P3-2. The body is not shipped. The code comment it matches (:139-143) is.

**FALSE-5. CHANGELOG.md:28-29: "One exception remains: when the agent then edits the file the lesson was about, that edit still counts as acting on it."**
This is TRUE as mechanism: hook.mjs:1233 `extractCiteBackSignals` → :1278 `citedMain.add`.
It is FALSE as a description of observed behaviour. The channel only credits ids parsed out of a rendered cite-back hint, and that hint
has 0 rendered instances on this machine (FALSE-3). The exception exists in code and has never fired here. Same-document tension:
line 53-54 says the hint never appeared.

**FALSE-6. 65a77bb body: "Any failed run (or unhandled error) now deletes the stamp."** Not for `--reporter` runs (P3-1).

## Checked TRUE
- **"only `activity get` bumps …; mem_get / get E#N do not"**: TRUE except the subcommand name (FALSE-1).
- **"tied first (6 each of 30)"**: matches §4.4.1 (:227 mutation vocab 6, :232 over-generalised 6, refactor 4, subagent 3, tool-failure 3).
  So "subagent crediting is 4th" holds, tied with tool-failure. I did not re-tag the 30 appendix rows.
- **"328 of 1,884"**: 328 TRUE, 1,884 FALSE (FALSE-2). Old vs new rule back to back: 328/328, removed 0, added 0. The commit's corpus claim reproduces.
- **"21 of 23 run a program, 2 pipe into bc"**:
  - Mechanism TRUE: `bc` is not in SEARCH_VERBS (bash-utils.mjs:15-48), and `grep -oP 'x' f.log | paste -sd+ - | bc` with an error-bearing
    response → `detectBashSignificance(...).isError === true`.
  - I did not re-run the count itself (26,406-result replay).
- **Sidechain sentence**: TRUE. `citation-stats --sidechain` → mem-cli.mjs:2802 `aggregateProjectCiteRecall` → citation-tracker.mjs:1358
  `computeThreadCiteRecall` → :1316 `includeDismissed: true`.
- **Adopt doc "排序上与未引用相同，同样下沉"**: TRUE. The decay loop's cited set is hook.mjs:1277 `extractCitationsFromTranscript(…, { mainOnly: true })`,
  which excludes dismissals, so an n/a-only id takes the uncited_streak branch like a silent one.
- **CHANGELOG "Known limit: … `sed -i`"**: TRUE. `sed -i 's/a/b/' f` and `cd repo && sed -i …` are both silent on error-bearing output.
- **"when the line's quotes balance; otherwise the old first-word rule"**: TRUE. The unbalanced `grep "x src/; python3 -` is silent via the first-word fallback.
- **hook-context.mjs:745-752 comment**: TRUE. The gate is `!isQuietHooks() && sessionStartEventsEnabled()`.
- **Audit §0 "5,009/9,096 = this project's sessions"**: consistent. Commands starting with `cd ` in this project's main transcripts up to 20:09Z:
  5,063/9,158 (55.3%), analysis session included. The claims review states the exact figure excluding it.
- **`无关(?!紧要)`** and the clause-end-at-next-id behaviour (`#12 n/a; #13 applied` → [13]): TRUE.

## Answers to the reporter questions
- Does a NORMAL full run still stamp? Yes, measured (see Baseline).
- Narrowing flags that still stamp:
  - `--config <alt>` that registers the reporter (P3-3);
  - `--root` + `--config` only with an absolute reporter path, which the repo's config does not use.
  - `--dir`, positional dir, `--exclude`, `--project`, `--tagsFilter`: refused or erroring.
- Clearing on any failed run: a filtered RED run (a TDD step) clears the stamp. Its only cost is that the next commit runs the suite
  (44 s), because stampPath is per worktree (`git rev-parse --git-path`). No bad interaction found. The gap is the opposite one:
  `--reporter` red runs do NOT clear (P3-1).

## NOT CHECKED
- The 21/23 split and the 26,406 / 893→765 / 151 figures (no full B1 replay run); the 65.8% → 46.0% citation-live-replay; 62.3 s → 12.1 s.
- The 30-row event labels and the per-shape tags (I relied on the doc's own §4.4.1 counts).
- claims-review.md and defect-review.md beyond the lines cited above.
- `--browser` (no provider installed), `--standalone`, watch-mode reruns, and concurrent runs (a red filtered run racing a green full run on one tree).
- Windows.
- Anonymous vitest SSR cache dirs my direct fixture runs may have left in /tmp. They cannot be told apart from other agents' dirs, so I did not delete them.
