> v6.13.2 发版前审阅报告，由独立子代理撰写，正文逐字保留。审阅范围 `ff62955..283bd20` 在审阅之后做过一次只改说明的 rebase（tree 逐个相同），SHA 对应：048e541 → d06dc32，e676fd7 → 4a6015c，cb5b981 → c25e376，283bd20 → a04f292。所有发现的修复见 1d6ccaa，该修复另经一轮增量审阅（20260926-v6.13.2-delta-review.md）。

# Pre-ship CLAIMS review — ff62955..283bd20 (v6.13.2 candidate)

Reviewer: claims lens. Clone: /tmp/claude-1000/claims-v6132-agv3/repo @ 283bd20 (verified `git log --oneline -5`).
Primary repo untouched (`git status --porcelain` empty afterwards, no stamp file there).
Live DB opened `{ readonly: true }` only. Scripts: scratchpad/claims-v6132-scripts/*.mjs.
Measured 2026-09-26 ~06:30–06:45Z. Where a cutoff is given, entries/files newer than it are excluded.

Tally: TRUE 21 · FALSE 3 · UNVERIFIABLE 1 (details below).

---------------------------------------------------------------------------------------------
## 1. key_files: "3 of 14 per-session windows lost 17 real files" (048e541 msg, hook-handoff.mjs:354-355, tests/handoff-obs-reach.test.mjs)

**Numbers: TRUE. Population: FALSE — the "per-session window" is not what buildAndSaveHandoff reads.**

scratch/kf.mjs + kf2.mjs replay old (`LIMIT 10` → isValidFile) vs new (stream to 10 contributing rows), same isValidFile.

| population | windows | affected | files lost |
|---|---|---|---|
| A. `WHERE memory_session_id = ?` alone, NO time window, NO `OR project` — cutoff 06:13:00Z | 14 | 3 | **17** (reproduces) |
| A, now (growth: +1 obs in manual-dev--claude-mem-lite since 06:13) | 14 | 3 | 18 |
| B. `(memory_session_id = ? OR project = ?)`, no window | 14 | 9 | 48 |
| C. product WHERE + product window, every mem session with prompts (310), bounded at session end | 310 | **0** | **0** |
| D. each of the 45 stored session_handoffs rows, window [first prompt of its cc session, handoff time] | 45 | **0** | **0** |

- The 14 = `SELECT DISTINCT memory_session_id FROM observations WHERE files_modified IS NOT NULL` (verified 14). 8 of them are `manual-<project>` ids.
- The 3 affected windows are all `manual-*` ids: manual-dev--claude-mem-lite (5), manual-dev--claudemd (1), manual-dev--code-graph-mcp (12 at now / 11+ at 06:13). hook-handoff.mjs's own comment (≈line 232) says the hook hands buildAndSaveHandoff `hook-<project>-<uuid8>`, never `manual-*`. So population A is the pre-namespace-widening, unwindowed read — neither `OR project = ?` nor `obsWindowClause` is modelled.
- Window sizes at handoff time (scratch/kf3.mjs): the largest reconstructed windows hold 10 and 11 files_modified rows; no stored handoff lost a file.
- Verdict: the D#40 shape is real (fix is still a correct latent fix, and the new read is a superset of the old one), but the measured "3/14, 17 files" describes a read the product does not perform. On the product's own read the live DB shows 0 losses. The sentence appears in 3 places: commit msg, hook-handoff.mjs:354-355, tests/handoff-obs-reach.test.mjs header ("Measured on the live DB ... over 14 per-session windows").
- Uncertain because: my /clear-row reconstruction (5 rows) takes the window from prompts carrying the handoff's session_id; if the product used the fallback window on those rows, their windows could be wider. Population C (all 310 sessions, fallback-style window) also reads 0, which bounds this.

Sub-claims:
- "Streaming keeps the filter exactly as it was" — TRUE (hook-handoff.mjs:363-376: same isValidFile; non-array JSON → `[]` new vs TypeError-caught old, both contribute nothing; new set ⊇ old set).
- "a tie came back ascending rowid and the cap kept the oldest rows" — TRUE on the repo schema: 5 rows same epoch, `ORDER BY created_at_epoch DESC LIMIT 3` → ids [1,2,3]; `+ id DESC` → [5,4,3] (both the files_modified and the `completed` WHERE shapes). Note: live DB has 0 created_at_epoch ties per project today (`GROUP BY project, created_at_epoch HAVING count>1` → []), so this fix has no current live effect (not claimed otherwise).
- "as 0ac7f04 did for Key Decisions" — TRUE (`git show 0ac7f04`: "Key Decisions breaks a created_at_epoch tie by id DESC").

---------------------------------------------------------------------------------------------
## 2. error-recall replay: "149 files, 14,979 results ... 0 silenced, 7 newly firing. Six heredoc-write + program run; one backtick in double quotes bash really executed" (cb5b981 msg)

**TRUE.** scratch/bu.mjs: old = `git show ff62955:bash-utils.mjs`, new = 283bd20; top-level + `<session>/subagents/*.jsonl` of -home-ai-dev-claude-mem-lite; exit-0 = `is_error !== true`; response = tool_result text; verdict = `detectBashSignificance(...).isHardError`.
- cutoff 06:23:00Z: files=149, results=14,974; cutoff 06:23:59Z: files=149, results=14,980 → 14,979 lies inside the stated minute (growth). silenced=0, fired=7 at both. Using toolUseResult stdout+stderr instead of content: same 0/7.
- Six fired commands are `cat >`/`cat >>` heredocs followed by `npx vitest run ...` (4) or `node <script>` (2) — verified by printing the post-terminator text (scratch/bu2.mjs).
- Seventh: `echo "=== consumers of `settings` in doctor ==="` — transcript 567e5e92 output contains `settings: command not found` and `=== consumers of  in doctor ===`: bash executed it. TRUE.
- Mechanism "apostrophe in heredoc body unbalanced quotes → first-word fallback silenced" — TRUE (old fallback `classifySimpleCommand(cmd.split('|')[0])`, the 6 cases are exactly that).
- Not in claims but noted: `CODE_GRAPH_READ_SUBCMDS` omits `outcome` (help text: "read-only") and `snapshot inspect`; these now count as runs. Comment says "The rest (serve, the index rebuilds, doctor's repairs, benchmark)" — the rest also includes outcome/adopt/unadopt/uninstall/snapshot. Minor wording gap, direction = fires more (safe).

---------------------------------------------------------------------------------------------
## 3. citation: "Over 3,012 #NN mentions in 217 top-level transcripts across every project ... exactly one verdict changed: the review's own example, quoted back" (283bd20 msg, lib/citation-tracker.mjs:119-122)

- 217 top-level transcripts — TRUE (cutoff 06:26:59Z: 217, one born after).
- "exactly one verdict changed ... the review's own example, quoted back in a report" — TRUE: the single flip (old dismissal → new not) is in -home-ai-dev-claude-mem-lite/c3c64219...jsonl, a Chinese report bullet quoting `"#12 irrelevant rows are now filtered" 这种句子仍会被判成否决`. Holds under every matcher tried. (Subagent transcripts, not claimed: 667 files, 531 mentions, 0 flips.)
- **"3,012 #NN mentions" — FALSE as a product-caliber count.** The product matcher (`citationIdRe()` = `(?<![A-Za-z0-9])#(\d{1,7})\b`, assistant `text` blocks — extractCitationsFromTranscript) gives **1,893** at 06:26:00Z and 06:26:59Z (1,892 at 06:20Z; stable, so not growth). An unanchored `#(\d{1,7})\b` over the same blocks gives 3,022–3,023; the extra 1,130 are `D#NN` (900, deferred-work ids) and `E#NN` (217, event ids) the product never treats as citations. I could not reproduce 3,012 exactly with any variant (closest 3,023). The same file's older comment (lib/citation-tracker.mjs:110-111) and CHANGELOG:40 quote "~1,880" on the product caliber, so two calibers now sit 10 lines apart in one comment block without saying so.

---------------------------------------------------------------------------------------------
## 4. green-stamp (e676fd7)

- "A run that does not load the stamp reporter (any --reporter=... run) now clears the stamp" — **FALSE as worded; TRUE for the parenthetical.** `--reporter=dot` under the root config: planted stamp removed (exit 0, 32 passed). Control, same file with default reporters: stamp kept. But a run under another config (`--config ../scratch/other.config.mjs --root . --reporter=dot`, 32 passed) does NOT load the reporter and did NOT clear the planted stamp — the globalSetup lives only in vitest.config.mjs. So "a run that does not load the reporter clears" holds only for runs of the root config. (scripts/green-stamp.mjs:23-27 comment has the same wording.)
- "A green one clears it too" — TRUE (the dot run above was green and cleared).
- "Only vitest.config.mjs at the root certifies" — TRUE by mechanism: probe reporter shows `ctx.vite.config.configFile` = `<root>/vitest.config.mjs`, `config.root` = clone root, so refusalReason's new check passes for the root config; full suite in the clone (432 files, 6732 passed, 1 skipped, 37 s) wrote the stamp and `checkStamp` returned reuse:true. A non-root config is refused by `resolve(configFile) !== resolve(root,'vitest.config.mjs')` (read, not run with a reporter-loading second config).
- "The stamp expires after 24 h" — TRUE: checkStamp with stamp age 0 h / 23.9 h → reuse:true; 24 h (+ms) / 25 h → "green stamp older than 24 h". Missing `at` → NaN → refused.
- "the repo has neither [symlinks nor gitlinks] today" — TRUE: `git ls-files -s` in primary → 0 entries with mode 120000/160000; no symlink among `ls-files -c -o --exclude-standard` paths.
- Environment note: the full suite first failed 1/6733 in my clone (`suite-touches-no-repo-files ... adopts the sandbox project — the premise`) because the clone's origin was a local path, not github.com/sdsrss/claude-mem-lite (dogfood detection). After `git remote set-url` in the clone only: 432/432 green. Not a product finding.

## 5. "A `--reporter=dot` run REPLACES the configured reporters" (vitest 5.0.0)

**TRUE.** Probe with `--reporter=default --reporter=<probe>`: resolved `ctx.config.reporters` = [default, probe] — the configured `./scripts/green-stamp-reporter.mjs` is gone. Consistent with the dot run clearing the stamp (setup saw no stamp reporter).

## 6. mem-cli.mjs / citation-tracker.mjs caliber comments

- `--sidechain` "counts a #NN n/a dismissal as an answer" — TRUE: mem-cli.mjs:2805 → aggregateProjectCiteRecall → computeThreadCiteRecall → `extractCitationsFromTranscript(p, { includeDismissed: true })` (citation-tracker.mjs:1332).
- per-face rates "including the subagent face, exclude dismissals since v6.13.0" — TRUE: citation_surface_log is written from `citedMain = extractCitationsFromTranscript(tp, { mainOnly: true })` (hook.mjs:1277) and `collectSubagentSurface` → `extractCitationsFromTranscript(p)` (default excludes); first tag containing 00effdc is v6.13.0. Nuance: hook.mjs:1278 adds cite-back (edit-hint) ids to citedMain regardless of an n/a — documented in CHANGELOG:38-40.
- classifyCitationContext "numerator counts a #NN n/a dismissal" — TRUE (citation-tracker.mjs:279-284: no isDismissalAt call).
- "benchmark/citation-live-replay.mjs reads this split" — TRUE: imports it (line 73) and calls `classifyCitationContext(path, { mainOnly: true })` at :211, reads `.withTool` at :222. (Note: plain `grep` treats that file as binary — use `grep -a`.)

## 7. README lines "`--sidechain` excepted: it counts an `n/a` as an answer" (EN + zh-CN)

**TRUE**, same evidence as 6; and "the CHANGELOG already stated" — TRUE (CHANGELOG.md:42-43).

## 8. Other stated numbers/mechanisms

- tests/bash-utils-signif.test.mjs comment "silenced whenever the body said "don't"" — TRUE (claim 2 mechanism).
- green-stamp-reporter.mjs "globTestSpecifications() resolves against it" — UNVERIFIABLE here (not run with a second reporter-loading config).
- scripts/green-stamp.mjs "`hash-object` follows a symlink" — not checked (see below).

---------------------------------------------------------------------------------------------
## FALSE (3)
1. 048e541 / hook-handoff.mjs:354 / tests/handoff-obs-reach.test.mjs: "3 of 14 per-session windows lost 17 files" is measured on `memory_session_id = ?` alone, unwindowed; on the product's read (OR project + obsWindowClause) the live DB shows 0/45 stored handoffs and 0/310 sessions losing a file. Numbers reproduce; population label is wrong.
2. 283bd20 / citation-tracker.mjs:121: "3,012 #NN mentions" is not the product's mention matcher (product: 1,893; the extra ~1,130 are D#/E# ids). Flip count (1) is correct under both.
3. e676fd7 / green-stamp.mjs:23: "a run that does not load the stamp reporter clears the stamp" — only under the root config; a `--config other --reporter=dot` run leaves a planted stamp in place.

## NOT CHECKED
- Whether `hash-object --stdin-paths` follows symlinks (P3-2 mechanism) and the gitlink `ls-files -s` parsing on a repo that has one.
- A red `--reporter` run specifically (inferred from globalSetup running before tests + green dot run clearing).
- Review-document references (delta/defect review P3-x ids) against docs/audit*.
- awk/sed/sort/find WRITES_OR_RUNS regexes beyond the shipped unit tests (suite green 432/432).
- Existence on disk of the 17 "real files" (I used isValidFile as "real").
- Any claim outside ff62955..283bd20.
