> Pre-ship review of 2ecaa1d..98911df for v6.13.0, by an independent subagent. All nine FALSE claims were re-measured and corrected in 46a6565 (files; its commit body itemises each correction, because the earlier commit bodies were left as written rather than rebased) and F3 also in e7c064e.

> Erratum (delta review): the subcommand that bumps `events.accessed_count` is `activity show`, not `activity get` as written in F2 below.

# Claims-lens pre-ship review — 2ecaa1d..98911df (v6.13.0)

Anchor: `git log --oneline 2ecaa1d..98911df` = 9 commits ending 98911df (checked).
Trees: extracted 98911df -> /tmp/claims-lens-gbVAvr, 2ecaa1d -> /tmp/claims-lens-pre-smpnSl (both removed at end).
Corpus: ~/.claude/projects/*/*.jsonl (214 top-level transcripts, 56 for this project), read with explicit globs; live DB opened readonly.
Reviewed 2026-09-25 ~21:08-21:35Z. Transcript corpora grow every session (doctrine rule 2), so a re-measured count counts as a reproduction when it lands within the drift the corpus shows.

TOOLING NOTE: `grep` in this shell is a ugrep wrapper with `-I` (it skips "binary" files). benchmark/citation-live-replay.mjs is classed binary, so a plain `grep` over it returns nothing. Every retraction sweep below used `command grep -a`. Any sweep that someone else runs with a bare `grep` has the same blind spot.

## Summary
TRUE 38 · FALSE 9 · UNVERIFIABLE 5

## FALSE

F1. "the section was injected 662 times into this project's main sessions"
    Where: ce1ea51 body; lib/events-injection.mjs:97; CHANGELOG v6.13.0 ("had been injected 662 times"); tests/e2e.test.mjs:1053 comment. The audit doc (§0 l.19, §4.4 l.207) says "662 行 Key Event" (662 rows), which is also wrong.
    Evidence: parsed SessionStart `hook_additional_context` attachments in this project's 55 main transcripts, excluding the analysis session. The `### Key Events` section was injected 50 times (unique by file + id list), carrying 250 E# rows (5 each).
    Where 662 comes from: the raw count of lines matching `- E#\d+ [` across those same 55 files is exactly 662. That count
    includes every SessionStart injection twice (hook_success + hook_additional_context record the same stdout), plus the
    /clear stdout echoes. So 662 is a grep count over duplicated records. It is not the number of injections, and not the number of rows.

F2. "accessed_count was 0 on all 3,776 event rows, i.e. no event had ever been opened by id"
    Where: CHANGELOG v6.13.0. Same inference in README.md:249 ("no event had ever been opened by id"); README.zh-CN.md
    (升级到 6.13.0: "从来没有任何一条 event 被按 id 打开过"); the env rows README.md:918 / README.zh-CN.md:705 ("never followed up /
    从未被跟进读取"); lib/events-injection.mjs:99 and hook-context.mjs:752 ("never followed up"); audit §4.4 l.206
    ("也就是 agent 从来没有按 id 取过任何一条 event").
    The number is TRUE: now 3810 rows, 0 with accessed_count>0. The inference is FALSE. The only writer of accessed_count is lib/activity.mjs:101 getEvent, and its only caller is cli/activity.mjs:134 (`activity get`).
    `mem_get` (server.mjs:961) and CLI `get E#N` (mem-cli.mjs:869) both go through fetchEventDetail (lib/get-core.mjs:124), which does not bump the count.
    The transcripts show events opened by id repeatedly: `node cli.mjs get E#860` (567e5e92, 2026-09-08; the result renders E#860),
    `get E#447`, `E#116`, `E#555`, `E#651` (cbe1121a, 09-07), `E#456` (eed3c6e1), `E#755` / `E#385` (f07d712d), `E#1923` (11db0684).
    That is 15 hits in total, several of them in this repo.

F3. "313 of 1,877 `#NN` mentions in assistant text are dismissals"
    Where: 00effdc body; lib/citation-tracker.mjs:109; CHANGELOG v6.13.0; audit §8 B2 row.
    Evidence: the shipped citationIdRe + isDismissalAt, run over the same 214 top-level transcripts, give 328 of 1876 at the 00effdc commit time (20:33Z), 327 of 1857 at 19:00Z, and 328 of 1881 now.
    The total reproduces, the dismissal count does not. No cutoff gives 313: the count is 327-328 from 19:00Z on.
    Dropping the gloss branch gives 324, and dropping the list-tail gives 203. So 313 most likely came from a draft rule, not the shipped one.
    The new count is about 5% larger, and that is the direction that strengthens the fix's case. Even so, the published figure does not describe the shipped classifier.
    (The CHANGELOG also says "every transcript on the maintainer's machine". The population is top-level transcripts only; subagent transcripts are not in it.)

F4. "(so the tested bytes are the committed bytes)"
    Where: 8d374b1 body. Also CHANGELOG "skips npm test when a full, passing, unfiltered vitest run already covered exactly the tree being committed", and audit §8 "有未暂存改动即不复用".
    The key includes untracked-not-ignored files (`ls-files -c -o`), and the reuse check only asks `git diff --quiet`, which covers tracked files.
    A file that the stamped run tested but that was never `git add`ed is therefore absent from the commit, and the stamp is still reused.
    Reproduced in a throwaway repo: main.mjs was committed with `import './helper.mjs'`, helper.mjs was left untracked, and the stamp was written.
    Result: checkStamp → {"reuse":true,"reason":"full suite passed on this exact tree ..."}, while `git status` shows `?? helper.mjs`.
    The script's own docblock (scripts/green-stamp.mjs:14-16) is correct: it says "tested TRACKED content". The three restatements drop that qualifier.

F5. "the 23 new ones each run a program behind a read-verb first word (a heredoc'd test and then node --test/pytest, or grep ...; python3 -)"
    Where: 68b44cd body; CHANGELOG ("each one runs a real program").
    Replay reproduced 23 added at a 20:00Z cutoff (script replay-b1.mjs, old and new detectBashSignificance back to back).
    21 of the 23 do run a program (node, pytest, docker, bash, python3 -c).
    2 of the 23 only read saved logs and pipe a count into `bc`:
      `f=.../test-a.log; grep -oP '^test result: ok\. \K\d+' $f | paste -sd+ - | bc; echo "suites=..."`
      `grep -E "^test result:..." .../b7olitq85.output | head -40; ... | paste -sd+ | bc; ...`
    Under the new rule these are new false positives: `bc` is not in SEARCH_VERBS. So "each" is false, and the new rule introduces some false positives of its own.

F6. "e.g. PreToolUse 65.8% → 46.0%", attached to "`citation-stats` and the funnel read lower from this version on"
    Where: CHANGELOG v6.13.0.
    The 65.8→46.0 reading comes from benchmark/citation-live-replay.mjs, as 00effdc says. citation-stats reads citation_surface_log rows written at Stop, which is a different ruler.
    I re-ran the replay: 65.0%→45.4% (156→109 over 240 pairs), which agrees within drift. The number is real; the sentence assigns it to the wrong ruler.

F7. "The dominant failure shapes are the summarizer reading mutation-probe vocabulary as product bugs and crediting subagent activity to the product."
    Where: ce1ea51 body.
    The audit's own tally (§4.4.1 "失败形状") gives: mutation vocabulary 6, over-generalisation / invented mechanism 6, refactor labelled "no behaviour change" 4, subagent/sandbox activity 3, own tool failure 3, historical comment 1.
    Subagent crediting is fourth, tied with own tool failure, so it is not dominant. Mutation vocabulary is tied for first with over-generalisation.
    (The CHANGELOG's "the most common failure is ... a deliberate test mutation" is at best tied-most-common. That one is imprecise rather than false, and not counted here.)

F8. "Gated on isQuietHooks() ONLY (explicit low-noise opt-out)"
    Where: hook-context.mjs:745-746.
    This comment predates the change. Since v6.13.0 the section is gated on `!isQuietHooks() && sessionStartEventsEnabled()` (hook-context.mjs:753).
    The paragraph added below it says "also opt-in", so the block now contradicts itself. The original sentence is FALSE for the current code.

F9. Audit §0 l.15: "本机 55% 的 Bash 命令带这个前缀"
    5009/9096 is this project's main-session transcripts, not the whole machine. §4.1 l.172 and the code comment (bash-utils.mjs "in this repo's transcripts") state the population correctly.
    Separately, §0 l.16 "约 26% 的 #NN 提及" (a ±80-char keyword window, this project only) and §8's 313/1877 (≈17%, shipped rule, machine-wide) are two calibers that are never reconciled. §7 does flag the 26.1% as approximate.

## TRUE (re-measured or verified in code)

T1. 5009 of 9096 cd-prefixed Bash commands in this repo's main-session transcripts. Exact: 9096 / 5009 excluding the analysis session c3c64219 (9343 / 5080 with it).
T2. B1 replay: 26,406 results, isHardError 893→765, 151 silenced / 23 new.
    My caliber (resp≥10) gives 896→765, 154 / 23 at 19:30Z, and 896→765, 154 / 23 at 20:00Z. Totals: 25.4k + 876 short ≈ 26.3k. That is a magnitude reproduction; the exact counts sit inside corpus drift.
    Arithmetic checks: 893−765 = 128 = 151−23.
T3. "all 151 silenced commands only read": in my silenced set (161 now) there is 0 `sed -i`, 0 redirect writes and 0 `awk system(`. That holds by construction, since every element must classify read/neutral.
T4. isReadOnlyCommand mechanics: statement split on `;` / newline / `&&` / `||` / `&`; pipeline split on `|` and `|&`; quote-aware; redirect `&` kept; unbalanced quotes fall back to the old rule; verb list exactly as stated (bash-utils.mjs diff).
    Mutants: removing `cd` from NEUTRAL, first-element-only, and splitting on redirect `&` were each killed by tests/bash-utils-signif.test.mjs. "5 cases" = 5 added `it(`.
T5. error-recall is gated on isHardError (hook.mjs:714), so the replay metric is the firing gate. error-recall-live-replay keeps only is_error or `^Error:|Exit code [1-9]` results (benchmark/error-recall-live-replay.mjs:151).
T6. citation-live-replay old vs new, back to back, same 214 transcripts / 135 sessions: pretool 156→109, error_recall 101→81 (claimed 100→80), fyi 39→28, ups 8→7. Rates match to ≤0.8pp. "79 of 303": 304−225 = 79.
T7. 30-row dismissal sample 30/30: my own independent seeded sample of 30 from the 328 was also 30/30 true dismissals.
T8. #54 is the most-cited row in this project (now cited_count 10; the claim said 9 at the time; the installed 6.12.2 keeps promoting it), and it was dismissed as n/a ("#137 / #260 / #518 / #54 / #779 n/a").
T9. Compliance callers pass includeDismissed:true (hook.mjs:1433 gate; citation-tracker.mjs:1302 computeThreadCiteRecall). Crediting callers use the default: access bump hook.mjs:1179, decay hook.mjs:1277, subagent citation-tracker.mjs:1286, live-replay.
    Mutants: gate without includeDismissed was killed by e2e "Stop counts a `#NN n/a` answer"; thread recall without it was killed by sidechain-cite-recall; access bump with includeDismissed was killed by funnel-e2e.
T10. "The reminder that nags ... still counts n/a as an answer": the gate uses includeDismissed:true, and the wide computeCiteRecall (citation-tracker.mjs:287) counts any `#NN`.
T11. tests/citation-surface-subagent.test.mjs fixture "Applying #11 to the fix; #22 n/a here." and the flipped expectation: confirmed in the diff.
T12. "citationIdRe does not match E#N": the `(?<![A-Za-z0-9])` lookbehind (citation-tracker.mjs:62).
T13. "Stop handler reads cite-back ids back with citationIdRe and unions them into BOTH the decay injected set and the cited set": CITE_BACK_ID_RE = citationIdRe() (cite-back-hint.mjs:425); hook.mjs:1234 injected.add; hook.mjs:1278 citedMain.add.
T14. lessonIdTokens: multiset split, obsIds absent → bare rendering; both re-render sites use it. Mutants: reverting the ack site was killed by pre-tool-recall "Read→Edit ack keeps the E# namespace"; reverting the cite-back site was killed by 2 cite-back-hint cases.
    Ack collision was frequent in practice: 372 id-mentions in 514 ack lines named an id previously injected as E# in the same session (heuristic).
T15. D#179/D#198: applyCitationDecay writes no importance on either branch (citation-tracker.mjs:1636-1671). Mutants: importance−1 on demote and importance+1 on promote were each killed by adopt-content-decay-claim. Reinserting the stale doc line was killed as well.
T16. "demote_pinned floors importance to 2 (1 without lesson)": PINNED_FLOOR_SQL (lib/maintain-core.mjs:516), predicate injection_count≥8 AND cited_count=0. It is in the default auto-maintain set (hook.mjs:1946), so "后台维护" (background maintenance) holds.
T17. Adopt text clause 1 (rank up / down, bounded multiplier): citeFactorClause clamp [0.4, 3.0], used in pre-tool-recall ranking (scripts/pre-tool-recall.js:652) and FULL_SCORE.
    Nuance: an uncited row's streak resets at 3, so it oscillates 1.0/0.75/0.5 rather than sinking monotonically.
    Clause 3 (n/a answers but does not raise): TRUE, since a dismissed id takes the uncited branch (streak +1). Verified end to end in the funnel-e2e case.
T18. "No schema change, no migration: an older build still opens the database": CURRENT_SCHEMA_VERSION = 49 in both trees; the schema.mjs diff is comments only.
T19. "Adopted projects pick up the new text at their next SessionStart": silentAutoAdopt runs every SessionStart, and needsRefresh compares detail-doc bytes (claudemd.mjs:208-221).
    Exceptions not stated: MEM_NO_AUTO_ADOPT=1, CLAUDE_MEM_NO_TEMPLATE_REFRESH=1, the per-project disable sentinel.
T20. "Only this face is paused": the only callers of recentInjectableEvents / sessionStartEventsEnabled are hook-context.mjs:753-757. The UPS leg (hook.mjs:3129) and the pre-tool-recall events query (scripts/pre-tool-recall.js:688) are ungated. mem_search still reaches events.
T21. "five newest importance ≥ 2 rows": limit 5 (hook-context.mjs:754), DEFAULT_MIN_IMPORTANCE 2, superseded excluded, ORDER BY created_at_epoch DESC.
    (No id tiebreaker, the CLAUDE.md invariant shape; not a claims issue.)
T22. MEM_QUIET_HOOKS keeps its meaning (still ANDed into the gate).
T23. e2e pins both arms: mutant gate always-on was killed by e2e "does NOT emit" and feature-sweep; mutant always-off was killed by e2e "when opted in"; dropping the gate at hook-context was killed.
    The flag parser has its own case (tests/events-injection.test.mjs); feature-sweep was restated.
T24. The events table count at the time (3,776) is consistent with now: 3810, all accessed_count 0, 2823 (74%) importance≥2. 933 non-superseded project events (932 with id≤3776 now).
T25. Event audit 2/11/16/1 of 30: recounted from appendix A (ACC 579,644; GENERIC 10; PARTLY 11; WRONG 16). Wilson CIs 1.8–21% and 36–70% recomputed.
    Spot-checks agree: E#308 (MEM_QUIET_HOOKS still at lib/quiet-scope.mjs:21); E#1106 (1bf34c3 raised FILE_PROBE_CAP 3→6); E#3771 (test 'tool input straddling 4000' live at tests/import-jsonl-dedup-scrub.test.mjs:162); E#3769 text as quoted.
T26. Pre-commit 395 commits / 44.2 s median / 4.35 h (09-05..09-25): exact. Bash `git commit` tool_use→tool_result durations over the 55 main transcripts give n=395, median 44.193 s, 4.35 h. The hook is wired (core.hooksPath=.githooks → scripts/pre-commit.sh).
T27. green-stamp contract: the reporter refuses a non-passed run, unhandled errors, -t, shard, --changed, --related, not-every-file, and key drift. The key is sha256 over blob ids of tracked + untracked-not-ignored regular files plus the node version. PRE_COMMIT_FULL_TEST=1 forces the run. eslint and format:check are unchanged and run first.
T28. "five mutations ... each turn it red": my five analogues were each killed by tests/green-stamp.test.mjs (untracked out of key, unstaged check removed, mid-run key check removed, full-run check removed, if-branch forced).
T29. 1934e25 "reproduced in tests/pre-tool-recall.test.mjs before the fix": reverting the fix line reddens that exact case (T14).
T30. CHANGELOG: sed/awk/ls/jq/diff added; "Every statement and pipeline element is now checked" (T4).
T31. README EN/zh upgrade sections and env rows agree with each other and with the CHANGELOG, apart from the shared F2 inference.
    Minor: EN/zh say "the file being edited / 正在编辑的文件", while the PreToolUse events leg also fires on Read. That is narrower than the truth but not false.
T32. Retraction sweep, importance ±1: no remaining "uncited loses 1 / cited gains 1" wording in any package.json#files entry, README*, or llms.txt. Swept with `command grep -a`, EN + zh patterns.
    Hits that remain are historical-and-marked (schema.mjs:62 "As shipped ... removed") or unrelated (boost `importance + 1`, demote_pinned). CHANGELOG history entries left as history.
T33. Retraction sweep, Key Events always-on: no statement in shipped files, llms.txt, commands/*.md, skill.md, or server instructions says SessionStart always shows Key Events. The only stale wording is hook-context.mjs:745 (F8).
T34. Audit §8 B1/B2 rows (restated numbers) agree with T2 and T6. The B3 row ("实体扫描另改 4 处注释": schema.mjs ×2, scoring-sql.mjs, user-prompt-search.js) matches the diff.
    The B4 row "2 个变异被杀" matches my own two killed arms. Its pre-commit row inherits F4.
T35. a6a28c0 body: "the retraction reached the code comments but not the one copy the MODEL reads": the pre-change adopt-content.mjs:92 carried it; the diff confirms.
T36. 00effdc "unit cases use shapes from the corpus": the fixture shapes (`#137 / #260 / #54 n/a`, `#1758、E#3520 与本次无关`) do appear in the corpus (my sample items 10 / 16).
T37. 68b44cd "The host resets the cwd between Bash calls, so agents prefix cd": consistent with 55% of this repo's commands (T1).
T38. The "Lessons #1 were shown" ack text shape matches scripts/pre-tool-recall.js:495.

## UNVERIFIABLE

U1. "On the same tree, back to back: 62.3 s → 12.1 s" (8d374b1 / CHANGELOG / audit §8).
    My reuse-path run in a sandbox git copy took 20.9 s (rc 0, "reusing green run"), under concurrent load from the defect-lens agent's suite runs. I did not run the forced arm. The direction is plausible; the absolutes are load-dependent.
U2. "All 6 mutants were applied ... each killed" (68b44cd, 00effdc), "2 / 1 / 3 mutants" (audit §8). These are process claims. My own analogue mutants: 3/3 (B1) and 3/3 (B2) killed, plus the others above.
U3. Cite-back leg of 1934e25 / CHANGELOG ("The same happened in the 'edited N file(s) with prior lessons' hint ... and there it also credited that unrelated observation").
    Mechanism: TRUE (T13). Occurrence: extractCiteBackSignals finds 0 cite-back ids in all 214 top-level transcripts, and the leader text "Cite-back:" appears on exactly 1 transcript line on the machine.
    So "the same happened" has no observed instance. The same applies to the code-level exception to "n/a now earns none of that": cite-back ids go into citedMain unconditionally (hook.mjs:1278), so a dismissed id that the hint also named would still be promoted. Measured reach is 0/254 dismissed-only (session, id) pairs.
U4. "30-row random sample (seed 20260925)" of events: the sampler and its labels are not in the tree, so I could not re-draw the sample. I verified the tally arithmetic and 4 verdicts (T25).
U5. B1 "is this analysis session misfired 6+ times" (audit §0). Not re-derived. (This review session was itself hit by the unfixed 6.12.2 exemption at least 8 times: "Related memories found for this error" after read-only cat/git commands.)

## Incidents during review
- The scratchpad is shared with defect-lens-v6130, and its `mut.sh` replaced mine. At about 21:25:24Z I invoked it with my own arguments.
  Its perl step failed (nothing was mutated anywhere), but it ran `npm test` in /tmp/defect-lens-RuEWY1 at the same time as that agent's M4 run, and wrote scratchpad/mut.hook.mjs.{log,fails}.
  I messaged defect-lens-v6130 to re-run M4 in isolation and to ignore those two files, and moved my own harness to a private temp directory.
- /tmp now holds 74 vitest `ssr` dirs newer than my start. They are a mix of mine and defect-lens's and cannot be attributed, so I did not delete them. /tmp is at 14% (1.6G/12G).

## NOT CHECKED
- Audit doc §2 / §3 global figures (84.9 h, 11,528 requests, token medians, language drift 26/223, 134 §8 denials, CI 74/75, the 45-release count, the §3.5 rows other than git commit), §4.5 M1–M8, and appendix A verdicts beyond the 4 spot-checks.
- The full suite on 98911df (only the 10 changed test files: 376/376 passed, plus targeted mutants).
- The forced pre-commit arm timing (U1).
- Whether `it.only` or a CLI `--exclude` can produce a stamp (a defect-lens question; the reporter compares ran vs globTestSpecifications, which honour the same filters).
- CHANGELOG / README prose outside the v6.13.0 entry and the two upgrade sections.
