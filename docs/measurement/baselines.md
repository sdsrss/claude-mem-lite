# Baselines — full record

Verbatim detail for the three baselines summarised in CLAUDE.md's **Baselines** table:
test-case count, knip dead-code, and coverage. Moved out of CLAUDE.md 2026-09-05;
text is byte-identical to v3.95.0 (commit ca1881b) except where a 2026-09-05
re-measurement is appended under its own heading.

## Test-case count — history and why it is partly generated


- **Test**: `npx vitest run` (**350 files / 5787**, re-measured 2026-09-04 on the **v3.95.0** release tree with `env -u CLAUDE_MEM_DIR`, after the version bump, the CHANGELOG entry and that round's pre-tag review repairs. Identical to the v3.94.0 tree's, which is the expected result for a dependency-only release with zero source and zero test-file changes — but it was re-run rather than carried, because v3.95.0 replaces the native `better-sqlite3` binding underneath every one of those cases, and "no JS changed" is not an argument that a rebuilt `.node` behaves the same. The v3.94.0 line, kept because the reasoning below is about that tree: measured 2026-09-04 on the v3.94.0 release tree, i.e. after the version bump, the CHANGELOG entry AND the pre-tag review repairs were all in place — the point of quoting a RELEASE tree is that it is the tree the tag will name, so measure it LAST. Measured at 5780 before those repairs; the +7 are the review's own additions, and quoting the pre-repair number under this tag would attribute the tag to a tree that still had the gaps. The +6 files / +51 cases against v3.93.1 include ONE generated case: the new `lib/env-number.mjs` moves `tests/obs-id-caliber-sync.test.mjs`, which is the standing reason this number cannot be carried. The v3.93.1 line read **344 / 5736**. The v3.93.0 TAG is 344 / 5735 and its parent `234dc48` reads 5726 — the +9 there were that release's pre-tag review repairs, and the +1 here is the post-release round. Three trees, three numbers; quoting the pre-review one under a tag's name attributes the tag to a tree that still had the defects. The v3.92.0 TAG is 337 / 5665 and the v3.91.0 line read 324 / 5524 — quote whichever tree you mean, they are three different trees and the tag is the one a user installs, vitest — and the case count is PARTLY GENERATED, so re-measure it rather than carrying it: `tests/obs-id-caliber-sync.test.mjs` emits one case per `.mjs`/`.js` file it finds under `benchmark/`, `lib/`, `scripts/` and the repo root, so adding a source file moves the headline number, and an untracked scratch file at the repo root moves it too. The 5330 written here through v3.85.0 did not reproduce: HEAD `63126ba` measures **5335** in both the primary tree and a clean `git worktree`, twice each. Re-measured 2026-09-01 at `4dbb39b` (v3.85.1): **5337** in a clean worktree, twice — and once, on a first run against a worktree sharing the primary tree's `node_modules` by symlink, **1 failure whose name was not captured**; two immediate re-runs of the same tree were fully green, so it is recorded as non-reproducing rather than as a flake, which would be a claim about a cause nobody established) · **Sandbox install harness** (not in `vitest run`; real `npm i -g` + real MCP stdio, minutes + network): `SBX_BASE=/tmp/claude/sbx node tests/sandbox/phaseA-plugin.mjs` / `phaseB-npm.mjs` / `phaseC-update.mjs`, one at a time — see `tests/sandbox/README.md`. **`SBX_BASE` is not optional decoration**: the harness falls back to `$TMPDIR`, which in a Claude Code session is `~/.claude/tmp/claude-<uid>`, i.e. under `$HOME` — and Node resolves `node_modules` up the tree, so on a machine whose `~/node_modules` holds `better-sqlite3` and `claude-mem-lite` (this one does) the run silently measures the home tree and passes anyway. The harness now refuses such a base (`tests/sandbox/sbx-base.mjs`, guarded by `tests/sandbox-base-guard.test.mjs`, which IS in `vitest run`). All three phases measured green 2026-09-04 on the v3.95.0 release tree — i.e. after the seven dependabot merges, including better-sqlite3 12.10.0 → 12.11.1, which is why this face was re-run at all: **44/44 · 45/45 · 15/15**, `SBX_BASE=/tmp/claude/sbx`, run separately, sandboxes 364 MB and deleted afterwards. **Phase A is 44 and not the 43 recorded below; that is a real check, not drift** — `c3cf6c9` ("A9 was driving an entry point that does not exist") added it after v3.90.0, and the delta was attributed by `git log v3.90.0..HEAD -- tests/sandbox/phaseA-plugin.mjs` rather than by subtracting two counts. Phase B is the one that answers "does the native binding still come up on a real `npm i -g`", and it does so under npm 12, whose **default install-script block means `npm ci` leaves better-sqlite3 with no `.node` at all** and a plain `npm rebuild better-sqlite3` prints "rebuilt dependencies successfully" without compiling anything — use `NATIVE_BINDING_REBUILD_CMD` (`lib/binding-probe.mjs:16`, i.e. `--dangerously-allow-all-scripts`). Earlier line: all three measured green 2026-09-02 at v3.90.0, before and after the guard change: **43/43 · 45/45 · 15/15**, sandboxes ~351 MB, left on disk by design


## Knip dead-code baseline — measurement contract


Knip baseline — **measurement contract, read this before comparing any number below (D#161 closed 2026-08-24 by fixing the method, not the discrepancy)**:

1. **Command + context are part of the number.** Measure with `./node_modules/.bin/knip` from the **primary working tree**. A `git worktree add --detach HEAD` checkout reads ~15 LOWER on the same commit with the same binary — reproduced, cause never established (candidates: gitignore evaluation differing per context; `knip.json` listing `tests/**/*.test.mjs` as `entry` while `project` excludes `tests/**`). It is not drift and it is not worth chasing; just never mix the two contexts.
2. **Never attribute a round's delta by subtracting two counts.** Run a **same-tree A/B**: `git show HEAD:<f> > <f>` for only the round's changed files, re-measure, restore, then diff **NAME SETS**. A count subtraction across contexts once read `+14 added` where the true delta was `-1 / +0`.
3. **A count is a smoke alarm; the name set is the evidence.** Treat every historical number in this paragraph as stale until re-measured.
4. **Reading the `--reporter json` output: every issue object carries a `files` key that is ALWAYS an array, empty or not.** So `d.issues.filter(i => i.files).length` counts every issue object (18 here) and reads as 18 unused files against a text report showing none — an empty array is truthy in JS, and the same predicate written in Python is falsy and returns the right answer, which is how the two disagreed loudly enough to be caught. Count the elements (`d.issues.flatMap(i => i.files || []).length`), and cross-check any file/export count against the text reporter before quoting it. `.github/workflows/ci.yml` prints both the count and the NAME SET on every push and PR, `continue-on-error` and gating nothing — it exists to answer, over a few rounds, which side of the ~15-name worktree/working-tree gap a fresh CI clone lands on. **Until that is answered, do not write a name-set guard**: one written today would be a bet, not a measurement. **First reading, n=1 (CI run 33660346977, v3.91.0): a fresh CI clone reads 53 — the same count as the primary working tree, and the NAME SETS are identical** (all 53 working-tree names appear in the CI output and CI printed exactly 53 lines, checked per rule 3 rather than by comparing counts). So CI lands on the working-tree side of the ~15-name gap, not the worktree side. One round is not "a few": a guard pinned to 53 today would be resting on a single observation of a discrepancy whose cause was never established, which is the same bet in a smaller disguise. The step writes its JSON to `$RUNNER_TEMP`, never the workspace, because an untracked file at the repo root moves the case count `tests/obs-id-caliber-sync.test.mjs` generates.

Current (2026-09-04, **v3.94.0 release tree**, measured per rule 1): **52** unused exports, **0** unused files — the same COUNT as the v3.93.0 line below, and the name set was read in full rather than inferred from the count: none of that release's **four** new exports is in it — `envNumber`, plus three test-only helpers in `tests/env-number.test.mjs`, which is outside the `project` glob. Counted with `git diff <prev-tag> -- '*.mjs' '*.js' | grep -E '^\+export '`, not from memory: a draft said six by assuming each new test file exported something, and five of the six export nothing. Rule 2 still applies to anyone attributing a FUTURE delta: this equality is two same-context measurements two days apart, not a same-tree A/B, so it establishes "nothing new went dead", not "nothing moved". Earlier line (2026-09-04, v3.93.0 release tree, measured per rule 1): **52** unused exports, **0** unused files. **The −1 against the previous 53 is attributed by `fcd93e5`'s own same-tree A/B name-set diff (−1 / +0), not by subtracting these two counts** — rule 2 forbids exactly that subtraction, and the two numbers here were taken two days apart: `ORPHAN_EPISODE_AGE_MS` left the list because `hook-llm.mjs` now imports it. Earlier line (2026-09-02, post-D#215-meter round, measured per rule 1): **53** unused exports, **0** unused files — and the same-tree A/B per rule 2 (baseline taken on the unmodified tree at the start of that session, same binary, same context) returned a **byte-identical name set (+0 / −0)**, so `lib/patha-exclude-meter.mjs`'s eight exports all have real consumers. **`inertMarkerIds` is the one that shows the check still works**: it landed with an intra-module caller and no importer, knip read **54**, and it was given a direct test rather than made private — the rule it encodes (`inert` keys on ids that are coercible AND string-typed) is the one a pre-tag review had just caught being wrong. `suppressedByWorkingExclude` is in the same position, reached only from `tests/patha-exclude-meter.test.mjs` — it is *also* called by `measurePathAExclude` in the same module, which is the point: a first draft inlined the filter there and would have shipped the metric row and the tests asserting on it as two implementations of one rule. Same number as the previous line (2026-09-02, post-D#207 round). **The +7 against the previous 46 is not drift and not new dead code — it is the blind spot in rule 4 being REMOVED, so knip is now reporting on seven modules it could not see at all.** Converting every `new URL('<relative>.mjs', import.meta.url)` in the tree to `join()` took the count 46 → 57; the eleven newly-visible names were `search-engine.mjs` ×7 (the FTS/count helpers + `ftsRowToResult` that category (a) below has always CLAIMED were in the list — they were not, the whole module was invisible), `hook-llm.mjs` ×2 and `hook-context.mjs` ×2. **The blind spot was SIX modules, not the seven a first draft of this line claimed** — `hook-context`, `hook-memory`, `search-engine`, `hook-semaphore`, `hook-llm`, `cli-path`, plus `lib/binding-probe` found by the new guard. **`hook.mjs` is invisible for a SECOND, independent reason that this round did NOT close and that also covers `server.mjs` and `install.mjs`: they are listed in `knip.json`'s `entry` array, and knip's `includeEntryExports` defaults to false.** Probe-verified both ways by the v3.89.0 pre-tag review: with `includeEntryExports: true` a probe export on `hook.mjs`/`server.mjs` appears, with the shipped config it does not. So a dead export in any of the three entry files is still invisible, and "hook.mjs became visible and has no dead exports" was never established. **The qualifier that sentence needs, because without it the next person goes looking for content that is not there: the hole is real and currently EMPTY.** Measured 2026-09-02 by running the same binary against a copy of `knip.json` with `includeEntryExports: true` and set-diffing the NAME sets (per rule 3): 53 → 66, and all **13** extra names come from `benchmark/*` and `scripts/*` — `episode-flush-replay` ×2, `keyctx-pool-replay` ×3, `error-recall-suite` ×3, `citation-live-replay` ×1, `scripts/prompt-search-utils.mjs` ×3, `scripts/user-prompt-search.js` ×1 (the first is `.mjs`, the second `.js` — a draft wrote both as `.js`). **Zero** from `hook.mjs`, `server.mjs` or `install.mjs`. So the blind spot is worth closing for the day one of them grows a dead export, not for anything it is hiding now. (`cli.mjs` is not in `entry` at all — it is reached as an import, so it was never in this blind spot.) Four of those were then made module-private rather than carried as a raised baseline (the v3.70.0 precedent, #9675): `KEYCTX_POOL_OBS` / `KEYCTX_POOL_SESS`, which the pool replay rewrites by regex over the file's TEXT and so never needed exporting, and `MEMORY_INPUT_GUARD` / `buildLessonRetryPrompt`, used only inside `hook-llm.mjs`. That leaves 53. Modules that became visible and turned out to have NO dead exports: `hook.mjs`, `hook-memory.mjs`, `hook-semaphore.mjs`, `cli-path.mjs`, `lib/binding-probe.mjs`. **Rule 4 below is kept as the error record, but its "the baseline is blind to those two files" caveat no longer applies, and `tests/knip-blindspot-guard.test.mjs` — the hand-written stand-in — has been DELETED: the real tool covers those modules now, and `tests/no-url-module-paths.test.mjs` guards the CAUSE (the path-construction form) for the whole tree with no allowlist.** Two things worth carrying: the blind spot was six modules, not the two D#194 knew about, and three of the sites were found by the new guard rather than by grep (`cli-path.mjs`'s own `'./cli.mjs'`, and two naming files one directory down) — a hand-written enumeration would have missed them. **A third: deleting `tests/knip-blindspot-guard.test.mjs` moved those two modules from an automated check to a manual one.** `knip` is wired only as `package.json`'s `dead-code` script — it is in no CI workflow, not in `scripts/pre-commit.sh`, and in no test — so `npx vitest run` used to go red on a new unconsumed export in `hook-context.mjs` / `hook-memory.mjs` and now nothing does. The deletion is still right (the real tool sees them again, and the other ~90 modules were never automated either), but it is a net coverage reduction for two files, not a pure win. Earlier line (2026-09-01, post-D#185/186/187/188/190/191 round, measured per rule 1): **46** unused exports, **0** unused files — and the same-tree A/B per rule 2 (park the round's three new files, `git show HEAD:` the eleven modified ones, re-measure, diff NAME SETS) returned a **byte-identical name set (+0 / −0)**. The round's new exports have real consumers: `injectedIdKey` by both hooks, `classifyCitationContext` and `mentionVsApplication` by the replay and its test, and the newly-exported self-checks in `rerank-pool-replay.mjs` and `keyctx-pool-replay.mjs` by their two new test files; the new benchmark is registered in `knip.json`'s `entry` list. **`KEYCTX_POOL_OBS` / `KEYCTX_POOL_SESS` are the exception and the name-set evidence says nothing about them** (v3.86.0 pre-tag review S7): nothing imports them — `benchmark/keyctx-pool-replay.mjs` rewrites their declarations with a regex over the file's TEXT — and a first draft of this line wrongly credited that as a consumer. **4. A module named in a `new URL('../X.mjs', import.meta.url)` anywhere in the analysed tree drops out of knip's unused-export report entirely.** Established by probe: appending `export const __KNIP_PROBE__ = 1;` to `hook-episode.mjs`, `utils.mjs` or `tier.mjs` makes it appear in the list; appending it to `hook-context.mjs` or `hook-memory.mjs` does not — and those two are exactly the modules the pool-replay benchmarks and their tests reference that way. Parking the benchmark alone leaves them blind, parking the test alone leaves them blind, parking BOTH makes the probe appear. So the baseline below is blind to those two files, and a dead export added to either is invisible; the technique is worth knowing before trusting a "+0 / −0" name set about a file a benchmark twin-patches. Cause is in knip's resolution, not in this repo — tracked with D#161. **[SUPERSEDED by D#207, 2026-09-02 — the file described in this sentence has been deleted; kept because its reasoning about text-vs-import-edge detection is still the right way to build such a guard.] The blind spot then had a stand-in guard (D#194): `tests/knip-blindspot-guard.test.mjs` resolved real import edges into those two modules — named imports plus `ns.NAME` reads through a namespace import — and fails on an export nothing imports. It is deliberately NOT a text search, because `KEYCTX_POOL_OBS` / `KEYCTX_POOL_SESS` do appear in `benchmark/keyctx-pool-replay.mjs` as a regex over the file's source, which a grep-based guard would miscount as a consumer; those two sit in an explicit allowlist instead, and the guard also fails if an allowlisted name GAINS an importer so the list cannot rot into a raised baseline. Mutation-verified in both directions, plus a self-check that the resolver can return "unconsumed" at all. It replaces nothing knip does elsewhere — the other ~90 modules are still knip's job.** **And the mechanism is now demonstrated rather than inferred, because a test written later the same day tripped it: adding `tests/pretool-event-id-namespace.test.mjs` — which read three source files via `new URL('../X.mjs', import.meta.url)` — dropped `lib/citation-tracker.mjs:extractInjectedFromSubagentPrompt` out of the knip name set (46 → 45), i.e. that one test file blinded knip to a whole module it had nothing to do with. Building the path with `dirname(fileURLToPath(import.meta.url))` + `join()` instead restored it, verified by a same-tree A/B reading +0 / −0. Practical rule: in a test that reads repo source as TEXT, use `join()`, never `new URL('../…')` — the URL form silently costs you knip coverage of whatever it names. Both guard tests now follow it.** Earlier line (2026-08-25, post-citation-live-replay/D#172 round, measured per rule 1): **46** unused exports, **0** unused files — and the same-tree A/B per rule 2 (park the round's new files, `git show HEAD:` the modified ones, re-measure, diff NAME SETS) returned a **byte-identical name set (+0 / −0)**. The round's new exports are all consumed: `wilson95` by three benchmarks, `IMPERATIVE_POOL_BACKSTOP` by `benchmark/imperative-pool-replay.mjs`, and `aggregate` / `assertFaceCoverage` / `assertRulerCanSayNo` / `runSelfChecks` by `tests/citation-live-replay.test.mjs`; both new benchmark entry points are registered in `knip.json`'s `entry` list. **A warning earned in this round: do not use `git checkout -- <f>` to undo a same-tree A/B when the tree carries uncommitted work** — it restores HEAD, which is indistinguishable from the A/B's own restore step, and it silently deleted this round's edits to both rewired benchmarks (they had to be redone). Park files by moving them; restore modified ones from a saved copy, not from git. Earlier line (2026-08-24, post-D#154/caliber round, measured per rule 1): **46** unused exports, **0** unused files — and the same-tree A/B per rule 2 returned a **byte-identical name set (+0 / −0)**, so that round added no dead exports (its two new exports, `OBS_ID_DIGITS` and `citationIdRe`, both have real consumers). Earlier line (v3.78.0, 2026-08-24): the working tree measures **46** unused exports and **0** unused files. The +1 against v3.77.0's 45 is `planErrorRecall` in `utils.mjs` — a v2.21 backward-compat re-export that lost its last consumer when `hook.mjs` stopped importing it (the extracted `lib/error-recall-core.mjs` takes it from `bash-utils.mjs`, the canonical source). That puts it in category (a) below alongside the ten other `utils.mjs` re-exports already in the list, so it is **not** removed. The round's own new exports are all consumed; three that were exported by habit and used only inside their module (`DEFAULT_FLOOR_REF_CORPUS`, `floorRefCorpus`, `maxIdf`) plus `ERROR_RECALL_HALF_LIFE_MS` were made module-private rather than carried as a raised baseline (the v3.70.0 precedent, cf. #9675), and `benchmark/error-recall-suite.mjs` was registered in `knip.json`'s `entry` list alongside the other standalone benchmark entry points rather than left reported as an unused file. Earlier line (v3.77.0, 2026-08-22): the working tree measured **45** unused exports, the same number v3.76.2 measured in the same tree shape, and none of that round's three new exports (`findSubagentTranscripts`, `collectSubagentSurface`, `NON_ATTACHMENT_SURFACES`) is in the list — each has a real consumer. Earlier line (v3.76.2, 2026-08-22): the parent commit `2ebc159` measured with the same `./node_modules/.bin/knip` reports **31** unused exports from a `git worktree add --detach HEAD` and **46** from the primary working tree; v3.76.2 itself measures **45** in the working tree (`recallForFile` left the list). A pre-tag review caught the first version of this sentence attributing the 46 to "the same commit" as the 45 — they are the parent and the child. The ~14-name gap is entirely `utils.mjs:12-15`'s backward-compat re-exports (`SYNONYM_MAP` / `FTS_STOP_WORDS` / `CJK_COMPOUNDS` / `expandToken` / `extractCjkSynonymTokens` / `extractCjkLikePatterns` / `TYPE_DECAY_CASE` / `resolveProject` / `_resetProjectCache` / `SECRET_PATTERNS`) plus their `nlp.mjs` / `registry-retriever.mjs` sources. Ruled out: knip cache (no `node_modules/.cache/knip`, and knip v6 has no `--no-cache`), stderr errors (none), untracked files (they would cut the count, not raise it). Not ruled out: gitignore evaluation, and `knip.json` listing `tests/**/*.test.mjs` as `entry` while `project` excludes `tests/**`. Tracked as D#161. **Every historical number in this paragraph is a worktree number**, so a plain working-tree run will read ~15 higher and that is not drift. The gap's composition was enumerated against the 46-name parent run, so treat the name list as indicative rather than exactly 14. **Attribute a round's delta with a same-tree A/B** — swap only the changed files for their HEAD versions, re-run, diff NAME SETS — not by subtracting a worktree number from a working-tree one; this round was misled by exactly that subtraction (it read +14 added; the same-tree A/B showed the true delta, **-1 / +0**, `recallForFile` leaving the list). v3.76.2 same-tree A/B: 46 -> 45 names, zero additions. Earlier line (2026-08-22, post-audit-P2 round): 0 unused files, **31** unused exports — re-measured after this round added `lib/fast-summary.mjs`, `lib/transcript-scan.mjs` and five new exports (`FAST_SUMMARY_LIMITS`, `TRANSCRIPT_CACHE_MAX_BYTES`, `RECOMMEND_MODE_UNIMPLEMENTED`, `getRequestedRecommendMode`, `upsFtsQuery`). NAME SET byte-identical to the previous measurement: zero additions, zero removals — every new export has a real consumer. Earlier line (2026-08-17, post-install-shape round / v3.70.0): 0 unused files, **31** unused exports — re-measured after adding `lib/install-shape.mjs` + `lib/hook-stdout.mjs`, NAME SET byte-identical to the pre-round set. Interim measurement read 34: the three extra were that round's own `DEFAULT_MARKETPLACE` / `DEFAULT_PLUGIN` / `MANAGED_ENTRY_POINTS`, exported by habit and used only inside their module — made module-private rather than carried as new baseline (cf. #9675). Earlier line (2026-08-17, post-e2e-round): 0 unused files, **31** unused exports — measured on the working tree, and the NAME SET is byte-identical to the pre-round measurement (zero additions, zero removals), so the round added no dead exports. The 2026-08-16 line below recorded 32 for the same tree shape; treat every number here as stale until re-measured. Earlier note: 0 unused files, **32** unused exports. Measured, not carried forward: the v3.68.0 batch scored 32 against **33** on the v3.67.0 tag, and a name-set diff between a clean-HEAD worktree and the working tree showed zero ADDITIONS — the single delta is `identifySynonymGroups` LEAVING the list (the new `tests/mcp-nonblocking-llm.test.mjs` imports it). Note the v3.67.0 line below recorded 31 while HEAD actually measured 33; prefer a fresh `npx knip` count over any number written here, and diff NAME SETS rather than counts when attributing drift. Earlier trail: 31→32 on 2026-08-14 (+1 = `BACKUP_EVICTION_GRACE_MS`, v3.63.0 M-9, intentional named constant, category (a)); down from 46 on 2026-07-17, itself down from 53 on 2026-06-29 — dead code cleaned in the v3.43 P3 batch, then OBS_TYPE_ENUM gained real importers in v3.49.0. No invocation-stats/dispatch name is in the list. Two categories:
(a) intentional — v2.21 utils.mjs split backward-compat re-exports + test-only
exports (search-engine.mjs FTS/count helpers, ftsRowToResult) used internally and
by tests; do NOT remove without audit. (b) NOT intentional — the v3 dispatch/
invocation-feedback CRUD in registry.mjs was confirmed dead (0 refs) and DELETED
in 2026-06; if invocation-stats functions reappear in this list they are rot from a
reverted feature, not back-compat. Plus 1 duplicate-name export pair
(FALLBACK_OBS_WINDOW_MS = RELATED_OBS_WINDOW_MS, intentional alias). Treat baseline
as the floor; flag NEW unused exports as PR review signal.


## Coverage baseline


Coverage baseline (2026-09-04, v3.93.0 release tree, `npx vitest run --coverage`, 344 files
/ 5735 cases green — NOT re-measured on the v3.93.1 tree, whose changes are four one-line
source edits plus test/doc text; treat it as the last measured figure, not as this tag's, exit 0 with no threshold ERROR line): statements **84.49%** · branches
**78.85%** · functions **89.27%** · lines **87.89%**, over all `lib/**/*.mjs` plus the
hand-picked root modules. Against the gate (statements 80 / lines 83 / functions 84 /
branches 74) that is **4.5–5.3pp of headroom on every axis**, so the gate is left where it
is — it was re-baselined once, and re-baselining it every round would turn a floor into a
follower. **Every one of the four moved up from v3.91.0's 83.73 / 78.06 / 88.52 / 87.13 and
the DENOMINATORS moved too**, because v3.92.0's successor batch put `registry.mjs` into the
`include` glob (P1-15): it was excluded on a "only exercised through subprocess E2E"
rationale that had expired — measured at 86.78% statements — so a well-covered file had
simply been invisible. `install.mjs` (11.67%) and `server.mjs` (25.89%) were measured too
and their exclusions HOLD; adding all three would have taken statements to 71.05% and blown
every threshold. **Quote percentages from the v8 text reporter, which is what the gate reads
— not from `coverage/clover.xml`**, whose `statements`/`conditionals` are a different
caliber and will not reconcile with these. Confirming this headroom was the one item the
v3.93.0 pre-tag test-effectiveness review left open: it established the gate is WIRED (a
one-file `--coverage` run exits 1 with all four ERROR lines) and that the denominators had
moved, but could not take a full run while the release suite held the tree. Earlier line
(2026-09-02, v3.91.0 tree, 324 files / 5524 cases): statements **83.73%** (7183/8578) ·
branches **78.06%** (5385/6898) · functions **88.52%** (918/1037) · lines **87.13%**
(6007/6894) — byte-identical to the v3.90.0 tagged tree's, and the reason is narrower than
the obvious one, which a v3.91.0 draft published and the pre-tag review refuted with a
counterexample. The draft said the change "lives entirely in `benchmark/` and `tests/`,
neither of which the glob covers, so 30 new cases could not move a single covered line."
**That inference is false: `tests/**` is never in the include glob, which is true of all
5524 cases, and tests are exactly what produces coverage of the files that ARE included.**
Run alone, `tests/patha-exclude-report.test.mjs` executes 91 lines of `lib/metrics.mjs` —
squarely inside `lib/**/*.mjs`. The true reason those eight numbers held: the new cases
reached only `lib/metrics.mjs` lines `tests/metrics.test.mjs` already covered, and no file
entered or left the glob. **A new test CAN move these numbers; living in `tests/` is not
what stops it.** **Which tree is a required field here, not a caveat**: a v3.90.0 draft
published the pre-round run (320 files / 5445 cases, 83.61 / 77.97 / 88.40 / 87.03) inside
that release's verification block, and the round's new `lib/` module matches the coverage
`include` glob — so the figure was inside the shipped scope and outside the measured one.
Still **outside** the gate and honest about it: `install.mjs` / `server.mjs` / `hook.mjs` /
`registry*.mjs` minus `registry.mjs` (exercised through subprocess E2E, which v8 coverage of
the parent process cannot observe) and `scripts/**` (same reason).


## 2026-09-05 re-measurement (v3.95.0 release tree, commit ca1881b)

First measurement taken on the v3.95.0 tree itself. Node v26.8.1 / npm 11.19.0
(the machine had no Node runtime at all before this date, so every figure above was
taken on a different toolchain — treat the toolchain as part of the stamp).

- **Tests**: `350 files / 5786 passed + 1 skipped (5787)`, 0 failures — identical to
  the v3.95.0 line recorded above, re-run rather than carried.
- **Knip** (primary working tree, per rule 1): **52** unused exports, **0** unused files
  — same COUNT as the v3.94.0 line, and the name set was read in full. Also reported:
  3 unlisted binaries (`du`, `claude-mem-lite`, `pgrep`, all in `install.mjs`) and the
  1 known duplicate-export pair.
- **Coverage** (`vitest run --coverage`, exit 0, no threshold ERROR): statements
  **84.37%** (7555/8954) · branches **78.88%** (5702/7228) · functions **89.22%**
  (977/1095) · lines **87.72%** (6302/7184). All four still 4–5pp above the gate
  (80/74/84/83). Denominators moved against the v3.93.0 line (8578 → 8954 statements),
  so this is not a same-population comparison with it.

### CI-clone vs working-tree knip: second reading (n=2)

The open question recorded under rule 1 — which side of the ~15-name worktree/working-tree
gap a fresh CI clone lands on — now has a second observation. Both are same-commit,
same-binary comparisons, and both were checked on the **name set**, not the count
(rule 3), by pulling the CI job log and diffing it against a local `--reporter json` run.

| Tree | CI fresh clone | Primary working tree | Name-set diff |
|------|----------------|----------------------|---------------|
| v3.91.0 (run 33660346977) | 53 / 0 files | 53 | identical |
| **v3.95.0 `ca1881b`** (run **33911262640**, job `101147970134`) | **52 / 0 files** | **52** | **identical, 0 added / 0 missing** |

So CI lands on the **working-tree** side in both rounds, not the worktree side. Retrieve a
future round with:

```
gh api repos/sdsrss/claude-mem-lite/actions/jobs/<lint-job-id>/logs | grep 'knip unused exports'
```

**This still does not license a name-set guard.** Two rounds of a discrepancy whose cause
was never established is a smaller bet, not a measurement — and both readings were taken
on release trees, so nothing here says a mid-round CI clone behaves the same. What n=2
does buy: the ~15-name gap is not something CI silently introduces, so a CI count can be
compared against a working-tree count from the same commit without the context caveat.
