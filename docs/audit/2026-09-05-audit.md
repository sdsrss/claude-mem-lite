# Audit — 2026-09-05 (round 1 of the baseline series)

Tree: v3.96.0 @ `d0045b2` + baseline-round additions (`docs/audit/00-baseline.md §2`).
Scope: every shipped `.mjs/.js/.sh` under root, `lib/`, `cli/`, `server/`, `scripts/`
(163 files) — read via the metrics ruler, the dependency extractor, four targeted acorn
scans, and hand reading of the entry points and every site cited below. **A finding
without a `file:line` was not made.** Report only; no source was changed in this step.

## 一句话总评

代码库处于“多轮审计后的收敛期”：0 循环依赖、eslint 0 错、跨文件重复 0.37%、上轮 P0/P1
全部关闭；剩下的是工程化门禁失效（prettier 从未执行）、审计产物不入库导致历史丢失、以及
一小批热路径上的循环内 `prepare`——都是小改动、低风险，没有发现可验证的 P0。

## 指标对比

Previous round's report (2026-09-02) is not on disk (`docs/` is gitignored), so the
comparison columns are: the v3.96.0 tag baselines recorded in `CLAUDE.md`, and this
session's same-tree run before the baseline additions (run A). Both measured 2026-09-05.

| Metric | CLAUDE.md (v3.96.0 tag) | Run A (today, pre-additions) | **This round** | Δ |
|---|---|---|---|---|
| Source files / lines | — | 163 / 52,095 | **163 / 52,182** | +87 lines (`--deps` mode) |
| Test files / cases | 350 / 5,825 | 350 / 5,824 + 1 skip | **351 / 5,831 + 1 skip** | +1 file / +7 cases |
| Functions > 50 lines | — | 137 / 2,019 | **138 / 2,034** | +1 (`depsMd`, tooling) |
| Duplicate rate any / cross-file | — | 1.97% / 0.37% | **1.96% / 0.37%** | 0 |
| Import cycles | 0 (`tests/import-graph`) | 0 / 0 | **0 / 0** | 0 |
| Coverage stmts / br / fn / lines | 84.34 / 78.87 / 89.25 / 87.67 | same | **same** | 0 (new test is outside the gate scope) |
| eslint errors / warnings | — | 0 / 0 | **0 / 0** | 0 |
| knip unused exports / files | 52 / 0 | 52 / 0 | **51 / 0** | −1, named: `install-metadata.mjs:MARKETING_ON_REQUEST` (now imported by the new test) |
| prettier unformatted files | — | 520 / 526 | **521 / 527** | +1 (the new test file, like the other 520) |
| Source modules not directly imported by a test / never mentioned | — | 25 / 4 | **24 / 3** | −1 / −1 (`install-metadata.mjs`) |

## 上轮（2026-09-02）问题状态

Reconstructed from CHANGELOG (v3.92.0 … v3.94.0) and code comments; the report itself
is gone. IDs are the previous round's.

| Item | Status | Evidence |
|---|---|---|
| P0-1 | **放弃（有意）** — premises refuted (host `SessionEnd` has no `exit` reason, fires on resume/logout, kill window) | CHANGELOG line 852-855 |
| P0-2 … P0-6, P1-1, P1-2, P1-11, P2-1 | 已解决 (v3.92.0 predecessors) | CHANGELOG 1165 |
| P1-3, P1-4, P1-5, P1-6, P1-8, P1-12, P1-13, P1-16, P2-12, P2-15(d) | 已解决 (v3.92.0) | CHANGELOG 1164 |
| P1-7, P1-9, P1-14, P1-15, P2-4, P2-5, P2-6, P2-7, P2-11, P2-13, P2-17, P2-19 | 已解决 (v3.93.x) — P2-17 (`no-shadow`) measured and **declined**, P2-13 half-corrected | CHANGELOG 851, 1122, 1135 |
| P3-13 | 已解决 (v3.94.0, `liveObsFilterSql` in decay/idle paths) | `search-scoring.mjs:319`, `lib/maintain-core.mjs:354` |
| P2-9 (“hook-shared is misnamed — server and two lib modules depend on it”) | **未解决** | `lib/lesson-bridge.mjs:5`, `lib/startup-dashboard.mjs:11`, allow-listed in `tests/observation-vector-single-writer.test.mjs:76-83` → this round P1-2 |
| P2-2, P2-3, P2-8, P2-10, P2-14, P2-15(a–c), P2-16, P2-18, other P3 | **无法核对** — no surviving text describes them | — |

复发：none detected. P1-4's guard (lib/ must not import the hook layer) holds for every
module except the named exception; the 0-cycle guard holds.

## 问题清单

### P0 — bug / 安全 / 数据风险

None found with evidence this round. (A single test failure appeared in one of four full
runs and did not reproduce — recorded under Uncertain, see P2-8.)

### P1 — 架构 / 重复 / 性能 / 工程化门禁

**P1-1 · 审计产物不入库，历史每轮蒸发**
- Evidence: `.gitignore:5-6` (`/docs/*` + `!/docs/measurement/`); `git log --all -- docs/audit docs/ARCHITECTURE.md` is empty; yet 41 code comments cite “audit 2026-09-02”, 34 cite 2026-08-14, and `tests/audit-metrics-selfcheck.test.mjs:1` says `docs/audit/*.md` quotes the ruler. The round you are reading had to rebuild the previous round's status table from CHANGELOG, and 9 of its P2/P3 items are unrecoverable.
- Impact: every round re-derives context; “复发” cannot be detected against a report nobody can read; CHANGELOG absorbs audit prose it was not designed for.
- Proposal: track `docs/audit/` and `docs/ARCHITECTURE.md` the way `docs/measurement/` is tracked (`!/docs/audit/`, `!/docs/ARCHITECTURE.md` in `.gitignore`), keep plans/specs local.
- Size: 2 lines + `git add`. Risk: low (docs only; nothing in `package.json#files` changes).

> **执行记录 (2026-09-05, 分支 `audit/2026-09-05-round2`, 第 1 项)**
> - 改动文件：`.gitignore`（`/docs/*` 后增 `!/docs/audit/`、`!/docs/ARCHITECTURE.md`，并改写上方注释说明为什么这三条必须被后续轮次读到）；`CLAUDE.md`（第 109 行的附录声明原本说“a fresh clone gets the appendix and nothing else from `docs/`”，改动后该句为假，改为列出三个被跟踪对象）；新入库 `docs/ARCHITECTURE.md`、`docs/audit/00-baseline.md`、`docs/audit/2026-09-05-audit.md`。
> - 验证方式：`git add --dry-run docs/audit/ docs/ARCHITECTURE.md` 逐个列出三个文件；`git status --short -uall docs/` 只列这三个，`docs/plans`、`docs/superpowers`、`docs/templates` 仍被忽略；`npx vitest run`；`npx eslint .`。
> - 结果：三个文件可入库且其余 `docs/` 仍忽略；测试 351 文件 / 5831 passed | 1 skipped；eslint exit 0。

**P1-2 · `hook-shared.mjs` is a hook-layer module that two `lib/` cores and the MCP server depend on (carried from 2026-09-02 P2-9)**
- Evidence: `lib/lesson-bridge.mjs:5` imports `callLLM`; `lib/startup-dashboard.mjs:11` imports `isAdoptedHere, HANDOFF_EXPIRY_EXIT`; `server.mjs` imports it too (dependency section, `docs/ARCHITECTURE.md §3`). `hook-shared.mjs:4-23` itself imports `haiku-client`, `schema`, `memdir`, `claudemd`, `adopt-content` — so a `lib/` module pulling one constant loads the whole hook constellation. The direction guard has to carve it out by name (`tests/observation-vector-single-writer.test.mjs:80`).
- Impact: the “lib/ does not depend on the hook layer” rule is true only by exception; `lib/startup-dashboard.mjs` cannot be unit-tested without the hook-layer import graph.
- Proposal: move `callLLM` and the session/handoff constants into `lib/` (e.g. `lib/llm-call.mjs`, `lib/session-constants.mjs`); `hook-shared.mjs` re-exports them so no caller changes; then delete `ALLOWED_HOOK_IMPORT` from the guard.
- Size: ~40-60 LOC across `hook-shared.mjs`, 2 new lib files, 2 lib importers, 1 test, plus `source-files.mjs` + `package.json#files` registration (the twice-shipped-broken-tarball rule). Risk: low-medium — import surface; the existing 5,831-case suite covers the callers.

> **执行记录 (2026-09-05, 分支 `audit/2026-09-05-round2`, 第 4 项)**
> - 改动文件：新增三个 lib 叶子模块（报告写的是“e.g. 2 个”，实际需要三个才能各自名副其实——把 `isAdoptedHere` 塞进一个叫 constants 的文件是错名）：`lib/llm-call.mjs`（`callLLM`）、`lib/quiet-scope.mjs`（`isQuietHooks` / `isAdoptedHere` / `effectiveQuiet`）、`lib/handoff-constants.mjs`（5 个 handoff 常量，零导入）。`hook-shared.mjs` 删除这些定义并改为 re-export，随之**不再导入** `haiku-client` / `memdir` / `claudemd` / `adopt-content`——这正是本条问题描述的“拉一个常量就加载整个 hook 星座”。`lib/lesson-bridge.mjs`、`lib/startup-dashboard.mjs` 改指新模块。`tests/observation-vector-single-writer.test.mjs` **删除 `ALLOWED_HOOK_IMPORT`**，用例改名为“no lib module imports a hook-layer module at all”。`tests/llm-timeout-budget.test.mjs` 的 `BACKGROUND_SITES` / `BG_TIMEOUT_USE_COUNTS` 把 `hook-shared.mjs` 换成 `lib/llm-call.mjs`（`timeoutMs = BG_LLM_TIMEOUT_MS` 默认值随代码搬走，hook-shared 已无调用点可守）。三个模块在 `source-files.mjs` + `package.json#files` 均已登记。
> - 顺带删除（同文件同问题）：`_extractResponseFromError` 全树零 importer，搬移后被 knip 数了两次；改为模块内私有函数并从 re-export 移除，而不是把一个死导出搬到新家。
> - 验证方式：变异验证（doctrine rule 5）——把 `lib/startup-dashboard.mjs` 的导入改回 `'../hook-shared.mjs'`，守卫报出 `lib/startup-dashboard.mjs -> hook-shared.mjs`（1 failed），还原后 10 passed，说明删掉例外后守卫仍然能说 NO。knip 做**同树 A/B 并对比名字集**（doctrine rule 2/4，均在 primary working tree）：`git stash` 前 51 → 改动后 52 → 删除死导出后 **50**，新增名字集为空，减少的名字集恰为 `_extractResponseFromError`。`npx vitest run`；`npx eslint .`。
> - 结果：测试 351 文件 / 5835 passed | 1 skipped；eslint exit 0；knip unused exports 51 → **50**，unused files 0；`hook-shared.mjs` 的 `callLLM` / `isQuietHooks` / `isAdoptedHere` / `effectiveQuiet` / 5 个 handoff 常量全部仍可从原路径导入（运行时逐个核对，9/9 present），server.mjs、hook-context.mjs、现有测试一行未改。

**P1-3 · Formatting is documented, configured, and never applied or gated**
- Evidence: `npm run format:check` → 521 of 527 files unformatted (`docs/audit/00-baseline.md §3`); `.prettierrc` was added 2026-09-03 (`62ac1b2 chore(baseline)`) with `printWidth: 110`; `scripts/pre-commit.sh:96-103` runs eslint + vitest only; `ci.yml` has no format step; `CLAUDE.md` lists `npm run format:check` as a project command.
- Impact: the command is a false signal (always red); the tree has no enforceable style, so every future “format on save” produces a 500-file diff by accident.
- Proposal (decision needed, two honest options): **(a)** one isolated `style:` commit running `npm run format`, listed in `.git-blame-ignore-revs`, then `format:check` added to `ci.yml` lint job and `pre-commit.sh`; or **(b)** delete the two scripts and `.prettierrc` and stop claiming it. Recommend (a) but **only in a commit that touches nothing else**, after the other items land.
- Size: (a) mechanical, ~520 files; (b) 3 files. Risk: (a) medium — blame noise, conflicts with any in-flight branch; correctness risk is bounded by the suite (prettier is AST-preserving).

**P1-4 · UserPromptSubmit runs two Node processes per prompt, coordinated through a marker file (carried; parked by an earlier round)**
- Evidence: `hooks/hooks.json:117,122` registers `scripts/user-prompt-search.js` (timeout 2 s) then `hook.mjs user-prompt` (timeout 5 s); `hook.mjs:2254-2260` (“Read IDs already injected by user-prompt-search.js”) and `lib/injected-ids.mjs` are the cross-process contract; each process opens `better-sqlite3` independently. CHANGELOG line 4035 records the same item parked as P1-4 in an earlier round.
- Impact: two cold Node starts + two DB opens on the hottest hook; a protocol that exists only because of the split (D#120 session-keyed marker).
- Proposal: **measure before deciding** — the hook telemetry (`doctor --metrics`, `lib/hook-telemetry.mjs`) already records per-hook wall time; quote p50/p95 for both processes. Only if the sum is material, fold `user-prompt-search.js` into `handleUserPrompt` as a `lib/` call and drop the marker.
- Size: if merged, ~150 LOC moved, 1 hook entry removed, 3 tests re-pointed. Risk: medium — the 2 s fast-path timeout is a deliberate contract; a merge changes what times out. **Not recommended this round.**

### P2 — 可读性 / 局部性能 / 测试

**P2-1 · 七个 300–550 行的函数**
- Evidence: `install.mjs:1601 doctor` 547; `schema.mjs:410 initSchema` 475; `hook.mjs:814 handleStop` 470; `scripts/user-prompt-search.js:630 main` 392; `hook-llm.mjs:757 handleLLMEpisode` 387; `hook.mjs:2119 handleUserPrompt` 326; `mem-cli.mjs:122 cmdSearch` 299. 138 of 2,034 functions exceed 50 lines (6.8%).
- Impact: `handleStop`/`handleUserPrompt` interleave five concerns each (stdin, flush, handoff, citation, nudge / stdin, prompt row, handoff injection, semantic injection, dedup) — the audit had to grep for phase comments to describe them.
- Proposal: split `handleStop` and `handleUserPrompt` into named phase functions **the way `handleSessionStart` was** (an earlier round's P1-10; `runSessionStartDbMutations`, `saveHandoffAndFastSummary`, `buildStartupDashboardText` are the pattern). Leave `doctor` and `initSchema` alone (linear scripts; see 不建议做).
- Size: ~200 LOC moved per handler, no behaviour change; `tests/feature-sweep-hooks.test.mjs` + `tests/handoff-simulation.test.mjs` are the net. Risk: medium — hot path, comment-dense; do one handler per commit.

**P2-2 · `db.prepare()` inside loops on two hot paths**
- Evidence: `search-engine.mjs:510` and `:530` prepare `SELECT … FROM observations WHERE id = ?` once per vector hit, inside `for (const vr of vecResults)`; `vecResults` is bounded by `VECTOR_SCAN_LIMIT` = 500 (`tfidf.mjs:21`). `scripts/user-prompt-search.js:421` prepares the file-recall query once per file named in the prompt. `hook-llm.mjs:490,497` (≤5 iterations) and 24 more sites are in one-shot migrations/maintenance and do not matter. `better-sqlite3` does not cache statements — each `prepare` recompiles.
- Impact: up to 500 compilations per hybrid search; measurable only on vector-heavy corpora, but it is the retrieval path.
- Proposal: hoist the statement above the loop (`const byId = db.prepare(...)`) at the three sites; optionally one `WHERE id IN (…)` for `search-engine.mjs`.
- Size: ≤10 LOC per site. Risk: low. Verify with `node benchmark/denoise-ab.mjs --save/--compare` (must read NEUTRAL) — results are order-identical by construction.

> **执行记录 (2026-09-05, 分支 `audit/2026-09-05-round2`, 第 2 项)**
> - 改动文件：`search-engine.mjs`（两个分支的 `db.prepare(SELECT ${VEC_HIT_OBS_COLS} …)` 上提为分支前的一条 `vecHitObs`——两个分支互斥，一条语句同时服务两边，因此是 −1 条语句而非 +1 个变量）；`scripts/user-prompt-search.js`（`searchByFile` 的整条 SQL 上提到 `for` 之外，`fileMatchParams(file)` 是绑定值不是 SQL，故循环不变）；`tests/vector-fieldset-r3.test.mjs`（原守卫断言“两条相同 SELECT”= `toBe(2)`，上提后只剩一条，改为断言“恰好一条共享语句 + 两个分支都经由它 + 循环内无 `db.prepare`”）。
> - 验证方式：守卫做了变异验证（doctrine rule 5）——把 `prepare` 放回循环 → 第 35 行失败（expected 2 to be 1）；把一个分支换成字面列清单 → 第 38 行失败（expected 1 to be 2）+ 行为孪生用例同时失败；还原后 5 passed。`node benchmark/denoise-ab.mjs --save`（**在 `git stash` 后的改动前树上**）→ `--compare`（改动后树），两臂背靠背同一语料（doctrine rule 2）。`npx vitest run`；`npx eslint .`；`npx vitest run tests/user-prompt-search.test.mjs`。
> - 结果：denoise-ab **VERDICT: NEUTRAL**，三个 suite 的 ΔR@10 / ΔP@10 / ΔnDCG / ΔMRR 全为 0.000，行为探针 cross-source 11/11、deferred 10/10、events-pipeline 9/9 全过；测试 351 文件 / 5831 passed | 1 skipped；eslint exit 0；`user-prompt-search.test.mjs` 131 passed。
> - 说明（doctrine rule 9）：denoise-ab 只驱动 `search-engine.mjs`，**看不到** `scripts/user-prompt-search.js` 那一处；该处的证据是 131 例的 `user-prompt-search.test.mjs` 与“SQL 文本循环不变”这一结构事实，不是 denoise-ab 的 Δ=0。

**P2-3 · One 12-line function shipped twice**
- Evidence: `lib/metrics.mjs:81-96 gcOldMetricShards(dbDir, retainDays)` and `registry-recommend.mjs:163-178 gcOldShadowShards(retainDays)` are the same daily-JSONL-shard GC, line for line, differing only in how the directory is resolved. Cross-file duplicate detector, largest non-intentional group.
- Impact: the retention rule (`retainDays = 90`, `YYYY-MM-DD.jsonl` name filter) has two homes — the twin-drift class this repo names in `CLAUDE.md`.
- Proposal: `lib/shard-gc.mjs: gcDailyShards(dir, retainDays)`; both callers become one-liners; register the module in `source-files.mjs` + `package.json#files`.
- Size: ~25 LOC net. Risk: low (both callers are SessionStart best-effort sweeps, `try/catch` around them).

> **执行记录 (2026-09-05, 分支 `audit/2026-09-05-round2`, 第 3 项)**
> - 改动文件：新增 `lib/shard-gc.mjs`（`gcDailyShards(dir, retainDays = 90)`，保留规则与 `YYYY-MM-DD.jsonl` 名称过滤的唯一住所）；`lib/metrics.mjs`（`gcOldMetricShards` 从 16 行缩为 2 行，只保留 metrics 目录解析——这正是两份代码唯一不同的部分）；`registry-recommend.mjs`（`gcOldShadowShards` 同理缩为 1 行）；两处删掉因此不再使用的 `unlinkSync` / `readdirSync` 导入；`source-files.mjs` + `package.json#files` 均已登记（CLAUDE.md 记录该登记漏过三次、发过三次坏 tarball）。两个导出函数的名字与签名不变，无对外行为变化。
> - 验证方式：变异验证（doctrine rule 5）——把 `gcDailyShards` 的删除条件改成恒假，`tests/metrics.test.mjs` 与 `tests/registry-recommend.test.mjs` **各失败 1 例**（expected +0 to be 1），证明两个调用方确实都走这一条路径而不是各自的残留副本；还原后 65 passed。`npx vitest run`；`npx eslint .`。
> - 结果：测试 351 文件 / **5832** passed | 1 skipped（比上一项 +1：`tests/obs-id-caliber-sync.test.mjs` 按源文件生成用例，新增一个 `lib/*.mjs` 就 +1 例，见 CLAUDE.md Baselines 注记，不是新写的断言）；eslint exit 0。

**P2-4 · Test reachability gaps that remain after this round**
- Evidence: `docs/audit/00-baseline.md §3` list — `cli/doctor.mjs`, `cli/activity.mjs`, `cli/fts-check.mjs`, `server/fts-check.mjs` are reached only as subprocesses by the sweeps; `stop-words.mjs` only transitively.
- Impact: low — the sweeps assert routing and output shape; a wrong number inside `cli/doctor.mjs --metrics` rendering would pass.
- Proposal: none this round; note for the next time `cli/doctor.mjs` changes (add an in-process case then).

**P2-5 · `catch {}` with no comment: 127 sites in shipped source**
- Evidence: `grep -rnE "catch(\s*\([^)]*\))?\s*\{\s*\}"` over shipped source → 127 (`install.mjs` 26, `hook.mjs` 20, `hook-episode.mjs` 9, `scripts/user-prompt-search.js` 8, `hook-semaphore.mjs` 7, `hook-llm.mjs` 7); 363 further catches carry a `/* reason */`. ESLint config allows it on purpose (`'no-empty': ['error', { allowEmptyCatch: true }]`).
- Impact: a swallowed error in `hook.mjs`/`install.mjs` is invisible — the exact class the v3.60 binding outage (`hook-errors/` telemetry) was added for.
- Proposal: **not a bulk edit** (an earlier round declined annotating 135 for the right reason). Route: when a file in the top-3 is next opened for a real change, convert its empty catches to `debugCatch(e, 'scope')` (`utils.mjs`) in that same change. Listed here so the number is tracked, not to schedule work.

**P2-6 · Audit IDs collide across rounds**
- Evidence: `tfidf.mjs:328` “v2.47 P0-1”, `schema.mjs:977` “v28 (v2.47) P0-1”, CHANGELOG 852 “2026-09-02 … P0-1” — three different defects share an ID; `hook-update.mjs:413` cites “P3-4” with no round.
- Proposal: prefix IDs with the round date in reports and in code comments (`A20260905-P1-2`). Size: convention only.

**P2-7 · The ruler discards vitest output on failure**
- Evidence: `scripts/audit-metrics.mjs runVitestCoverage()` keeps only the regex-matched summary; run B of this baseline reported `1 failed` and the name is unrecoverable (`00-baseline.md §4`).
- Proposal: on non-zero exit, write the last ~200 lines of vitest output to `tmp/audit-vitest-<ts>.log` and print the path in the Markdown table. Size: ~10 LOC in tooling. Risk: none.

> **执行记录 (2026-09-05, 分支 `audit/2026-09-05-round2`, 第 5 项)**
> - 改动文件：`scripts/audit-metrics.mjs`（`runVitestCoverage()` 在 `status !== 0` 时把 `FAIL` 行解析成 `failed[]` 并把输出末 200 行写入 `tmp/audit-vitest-<ts>.log`，`md()` 的 vitest 行在红的时候追加失败用例名与日志路径；写日志整体 try/catch，失败降级为 `unwritable: <msg>` 而不是让 ruler 崩掉）。比报告多做一步：**名字直接进报告行**，不只是日志路径——doctrine rule 4“计数是烟雾报警器，名字才是证据”，只给路径仍然要求读者去翻文件。
> - 验证方式：把 ruler 驱动到失败（doctrine rule 5）——临时放入一个必失败的 `tests/zz-temp-ruler-probe.test.mjs`，跑 `node scripts/audit-metrics.mjs --run-tests --md`。
> - 结果：红跑输出 `| vitest | Test Files 1 failed | 351 passed; Tests 1 failed | 5835 passed; 32.12s (exit 1) — **failed: tests/zz-temp-ruler-probe.test.mjs > TEMPORARY ruler probe — delete me > deliberately fails so audit-metrics P2-7 can be driven to failure**; output tail: `tmp/audit-vitest-2026-09-05T18-25-12-778Z.log` |`，日志 103 行、含 2 处 `AssertionError`/`FAIL`；删掉探针后绿跑那一行回到 `(exit 0)` 且不含 failed/log 字段。探针文件与日志已删除（§8.V4），`tmp/` 残留 0 项。`npm run audit:selfcheck` → OK；`npx vitest run` 351 文件 / 5835 passed | 1 skipped；`npx eslint .` exit 0。
> - 对 P2-8 的影响：本轮 7 次全量跑（含 coverage）**未复现**那次失败，所以 P2-8 仍然是 Uncertain；但下一次发生时名字会自动落到报告行里，可归因。

**P2-8 · Uncertain: one non-reproduced test failure**
- Evidence: 4 full runs today — A green, **B: `Tests 1 failed | 5830 passed` (exit 1)**, C green, D green (with coverage). Same tree between B, C, D.
- Uncertain because the name was lost (P2-7). Treat as a flake candidate; P2-7's fix is what makes the next occurrence attributable.

**P2-9 · 依赖标尺把注释掉的 `import()` 当成一条边（本轮新发现，未处理）**
- Evidence: `scripts/audit-metrics.mjs:249-262 edgesOf()` 直接对源文件**原文**跑 `STATIC_IMPORT` / `BARE_IMPORT` / `DYNAMIC_IMPORT` 三个正则，不剥注释；`lib/save-enrich.mjs:175` 是一行注释 `// This used to \`await import('../hook-optimize.mjs')\` for its \`rebuildVector\` — the …`，于是 `npm run audit:deps` 至今把 `lib/save-enrich.mjs → hook-optimize.mjs (lazy)` 列进 upward edges。对照组：`tests/observation-vector-single-writer.test.mjs` 在匹配前先 `.filter((l) => !/^\s*(?:\/\/|\*|\/\*)/.test(l))`，所以守卫是绿的而标尺是红的——两个"同一条规则"的读数互相矛盾。
- Impact: `docs/ARCHITECTURE.md §3` 的边数与 upward-edge 清单会高报；同一套提取喂给 `cycles()`，**理论上一条注释掉的 import 就能造出一个不存在的环**（当前 0/0，未发生）。这正是 doctrine rule 5「标尺必须能说 NO」的反面：它会说不该说的 YES。
- Proposal: `edgesOf()` 复用守卫那一行的剥注释过滤（或改用已在本文件里的 acorn 走 AST）；然后重新生成 §3 并对比**名字集**而不是边数（doctrine rule 4）。
- Size: ~3 LOC（过滤）或 ~15 LOC（acorn）。Risk: low，但会改动一个被文档引用的数字，需同轮更新 `docs/ARCHITECTURE.md §2/§3`。

> **执行记录 (2026-09-05, 分支 `audit/2026-09-05-round3`, 第 1 项)**
> - 改动文件：`scripts/audit-metrics.mjs`——`edgesOf()` 改走 acorn AST（`ImportDeclaration` / `ExportNamedDeclaration` / `ExportAllDeclaration` / `ImportExpression` 的字面量 source），**不是**剥注释的正则。选 AST 而不是报告里更便宜的"~3 LOC 过滤"，是因为剥注释本身要处理"字符串里含 `//`"这类反例，而注释与字符串在 AST 里根本不是节点。三个旧正则保留为**解析失败时的兜底**：返回零边比返回多余边更危险——没有边的模块不可能参与环，一次解析失败会读成一张干净的图；兜底触发的文件名会印在 §3 里。`--self-check` 新增第 4 组检查（YES 臂：真实静态/动态 import 必须被看见；NO 臂：注释掉的 import 与字符串字面量里的 import 都不算边）。`docs/ARCHITECTURE.md` §3 重新生成，§2 的相应段落改写。
> - 验证方式：同树 A/B 并**对比边的名字集**（doctrine rule 4）——`git stash` 掉标尺改动跑一次，恢复后再跑一次。三处变异验证：把提取换回正则 / 关掉动态 import 臂 / 关掉静态 import 臂。另外把真实 `await import('../hook-optimize.mjs')` 临时加回 `lib/save-enrich.mjs` 确认仍被看见。`npx vitest run`；`npx eslint .`；`npm run audit:selfcheck`。
> - 结果：static edges **481 → 481（无变化）**，lazy edges **49 → 48**；名字集差集：删除 1 条 `` `lib/save-enrich.mjs` → `hook-optimize.mjs` (lazy) ``，新增 0 条——即整条改动只影响那一个幽灵边，没有连带漏掉任何真实边。变异 1 报 `edgesOf counted a commented-out import as an edge (0 static, 1 lazy)`；变异 2 报 `edgesOf missed a real dynamic import`；变异 3 报 `edgesOf missed a real static import`；未变异时 `audit-metrics self-check: OK`。`lib → engine` 12 条（round 2 记的 13 条里有 1 条是这个幽灵）。测试 351 文件 / 5835 passed | 1 skipped；eslint exit 0。
> - 过程中撞到的事：self-check 的探针最初把 `'./edge-target.mjs'` 写成字面量，`tests/import-graph.test.mjs` 立刻报 `scripts/audit-metrics.mjs -> ./edge-target.mjs: unresolvable`——**同一个缺陷的第二个住所**当场自证。按规则未在本项处理，改为把探针的 specifier 用变量拼装，并把第二个住所登记为 P2-10。

**P2-10 · 同一个"扫文本当依赖"的缺陷还有第二个住所：环守卫自己（round 3 新发现，未处理）**
- Evidence: `tests/import-graph.test.mjs:34 SPEC_RE` 对整份源文件文本做 `matchAll`，`pathSpecs()` 只按 `.` / `/` 开头筛选，不区分代码、注释还是字符串字面量。本轮把 `'./edge-target.mjs'` 写进 `scripts/audit-metrics.mjs` 的一个 self-check 探针字符串后，该测试立刻红：`scripts/audit-metrics.mjs -> ./edge-target.mjs: expected [ …(5) ] to deeply equal []`——它把一个字符串常量当成了 audit-metrics.mjs 的真实 import。
- Impact: 这是本仓库的**环守卫**（`buildGraph` → 环检测）与"每个相对 import 都可解析"检查的共同提取器。一条注释掉的相对 import 指向已删除文件，会让"unresolvable imports"误报；一条注释掉的 import 指回上游模块，则可能让守卫报出一个**不存在的环**——正是 P2-9 在标尺侧刚修掉的那一类。今天没发生，是因为在此之前没有源文件的字符串里出现过像 import 的文本。
- 反面论据（值得先想清楚再动手）：对"unresolvable import"这一项，宁可多报也不漏报是合理的保守设计；换成 AST 会让"注释里写着一个已删除路径"从红变绿。所以这不是纯粹的 bug 修复，而是要**分开处理两个断言**：环检测应当用 AST（多余的边会造出假环），可解析性检查可以保留文本扫描。
- Proposal: `buildGraph()` 的边提取改用 AST（可直接复用 `scripts/audit-metrics.mjs` 里刚写好的形状）；`resolves every relative import` 那一条维持文本扫描但把注释行滤掉。改完必须**对比名字集**而不是边数。
- Size: ~20 LOC。Risk: low-medium——动的是环守卫本身，必须变异验证（人造一个真环，确认仍然红）。

### P3 — 风格 / 环境

**P3-1 · Local Node 26 vs CI matrix 20/22/24** — `node --version` v26.8.1; `ci.yml` matrix `[20, 22, 24]`; `engines >= 20`. Every local green is on a version CI never runs. Proposal: add 26 to the matrix (or pin local via `.nvmrc`). Size: 1 line.

**P3-2 · Two majors available** — `better-sqlite3` 12.11.1 → 13.0.3, `vitest`/`@vitest/coverage-v8` 4.1.11 → 5.0.0, `eslint` 10.9.1 → 10.10.0. Proposal: eslint minor whenever convenient; the majors each need their own release with the FUSE/native-binding checklist (`.gitignore:12-18`, CHANGELOG v3.95.0). Not this round.

**P3-3 · Six functions with > 5 positional parameters** — `hook-handoff.mjs:35`, `lib/edge-attribution.mjs:88`, `scripts/user-prompt-search.js:348`, `search-engine.mjs:295,313`, `server.mjs:249`. Convert to an options object only when touched.

## 本轮建议执行的前 5 项（收益/风险比最高、互不冲突）

1. **P1-1** — un-ignore `docs/audit/` and `docs/ARCHITECTURE.md` (2 lines). Everything below becomes checkable next round because of this.
2. **P2-2** — hoist the three hot-path `prepare` calls (`search-engine.mjs:510,530`, `scripts/user-prompt-search.js:421`), verify NEUTRAL on `denoise-ab`.
3. **P2-3** — `lib/shard-gc.mjs`, collapse the GC twin (+ registration in `source-files.mjs` / `package.json#files`).
4. **P1-2** — move `callLLM` + session constants into `lib/`, keep `hook-shared.mjs` re-exports, delete the guard's named exception.
5. **P2-7** — make the ruler keep vitest output on failure, so P2-8 can be closed or confirmed.

Deliberately not in the five: P1-3 (needs the user's (a)/(b) decision and its own commit,
because it conflicts with every other diff), P1-4 (measure first), P2-1 (one handler per
commit, after the small items).

## 明确不建议做

- **Splitting `install.mjs:doctor` (547) or `schema.mjs:initSchema` (475).** Both are linear, once-per-run scripts; `doctor` deliberately shadows `ok/warn/fail/log` to make `--json` work (`eslint.config.mjs` comment) and `initSchema` is the migration ledger. Splitting buys files, not guarantees.
- **Bulk-annotating the 127 empty catches** — declined once with reasoning that still holds; rubber-stamp comments are not error handling.
- **Merging the two UserPromptSubmit processes without telemetry numbers** (P1-4).
- **Re-layering `lib/` under `engine`.** The 11 `lib → engine` edges are shared cores fronting their engines (`docs/ARCHITECTURE.md §2`); “fixing” them means inverting a design `CLAUDE.md` states on purpose.
- **Bumping `better-sqlite3` or `vitest` majors inside an audit-fix release.**
- **Adding `install.mjs` / `server.mjs` / `hook.mjs` to the coverage gate.** Measured 2026-09-03 at 11.67% / 25.89% statements in-process; they are exercised by subprocess E2E that v8 cannot see (`vitest.config.mjs` comment). Including them measures the harness.
- **Cleaning the 51 knip exports as a batch.** The intentional category (`utils.mjs` v2.21 re-exports, test-only exports) is documented in `CLAUDE.md`; treat NEW names as review signal only.
- **Reformatting the tree as part of any other change** (P1-3 option (a) must be its own commit).

---

# 本轮变更摘要（round 2 — 执行轮，分支 `audit/2026-09-05-round2`）

本轮按"本轮建议执行的前 5 项"逐项执行，一项一提交，每项跑完整测试 + eslint 才进入下一项；
**5 项全部完成，0 项回滚**。每项的改动文件 / 验证方式 / 结果记在上面对应条目下的「执行记录」里。

| # | 条目 | 提交 | 净效果 |
|---|---|---|---|
| 1 | P1-1 审计产物入库 | `8214162` | `.gitignore` +2 条否定；`docs/audit/`、`docs/ARCHITECTURE.md` 入库，其余 `docs/` 仍本地 |
| 2 | P2-2 热路径循环内 `prepare` | `1e21e3a` | 3 处 → 0 处；`search-engine.mjs` 两条孪生 SELECT 合并为 1 条共享语句 |
| 3 | P2-3 shard-GC 孪生 | `85423f7` | 新增 `lib/shard-gc.mjs`，两份 12 行副本 → 一份；两个调用点各缩为 1–2 行 |
| 4 | P1-2 `hook-shared` 反向依赖 | `727b94a` | `lib/` 对 hook 层的依赖 2 → **0**，守卫的 `ALLOWED_HOOK_IMPORT` 例外删除；顺带删掉 1 个死导出 |
| 5 | P2-7 标尺丢弃失败输出 | `2cb0a41` | 红跑现在把失败用例名写进报告行，并把末 200 行存到 `tmp/audit-vitest-<ts>.log` |

## 指标对比

两列都是**本次会话内背靠背跑出来的**，不是引用上一轮的旧数字（doctrine rule 2）：结束列
`node scripts/audit-metrics.mjs --run-tests --md` 跑在 primary working tree 的 `2cb0a41`；
开始列用 `AUDIT_METRICS_REPO` 指向 `b3b07d8` 的 detached worktree 跑同一把标尺的 `--no-tools`
静态部分。**knip / prettier / coverage 三行的开始值不取自那个 worktree**——`CLAUDE.md` 记着
worktree 的 knip 会低约 15，两种上下文不能混——它们的 Δ 另有来源：knip 是第 4 项里做的
同树 A/B（51 → 50，名字集差集单一），coverage 与 prettier 取本轮开始时在 primary tree 的实测。

| Metric | 本轮开始（`b3b07d8`） | 本轮结束（`2cb0a41`） | Δ |
|---|---|---|---|
| Test files / cases | 351 / 5,831 + 1 skip | **351 / 5,835 + 1 skip** | +4 例（`obs-id-caliber-sync` 按源文件生成，本轮新增 4 个 `lib/*.mjs`） |
| Source files / lines | 163 / 52,182 | **167 / 52,282** | +4 文件 / +100 行 |
| eslint errors / warnings | 0 / 0 | **0 / 0** | 0 |
| knip unused exports / files | 51 / 0 | **50 / 0** | **−1**，名字集差集 = `_extractResponseFromError`（同树 A/B，primary working tree） |
| Import cycles static / lazy | 0 / 0 | **0 / 0** | 0 |
| Duplicate rate any / cross-file | 1.96% / 0.37% | **1.88% / 0.29%** | **−0.08 / −0.08 pp**（P2-3 与 P2-2 各去掉一处逐行重复） |
| `lib/` → hook 层依赖 | 2（例外放行） | **0（无例外）** | −2 |
| Coverage stmts / br / fn / lines | 84.34 / 78.87 / 89.25 / 87.67 | **84.34 / 78.88 / 89.26 / 87.67** | +0.00 / +0.01 / +0.01 / +0.00 pp |
| prettier unformatted files | 521 / 527 | **525 / 531** | +4（本轮新增的 4 个文件与其余 521 个一样未格式化；P1-3 未做） |
| Functions > 50 lines | 138 / 2,034 | **139 / 2,036** | +1（`runVitestCoverage` 因 P2-7 越过 50 行） |
| 未被任何测试直接 import 的源模块 | 24 / 163 | **28 / 167** | **+4**——4 个新模块都只被生产代码 import，没有直属单测（见下方"遗留与新增"） |

一句话：**收敛期继续收敛**——重复率与死导出双双下降、`lib/` 的层级规则第一次做到"零例外"，
没有引入新依赖、新配置项，也没有对外行为变化（CLI、MCP 工具、数据格式、hook 契约均未动）。

## 遗留与新增

- **未执行（报告已明确排除在前 5 之外）**：P1-3（prettier，需用户在 (a)/(b) 间决策且必须独占一个提交）、
  P1-4（两个 UserPromptSubmit 进程，需先量 telemetry）、P2-1（七个大函数，一次一个 handler）、
  P2-4/P2-5/P2-6、P3-1/P3-2/P3-3。
- **本轮新增问题**：P2-9（依赖标尺不剥注释，见上），按规则只登记不处理。
- **本轮自己造成的一处退化，如实登记**：`lib/shard-gc.mjs`、`lib/llm-call.mjs`、
  `lib/quiet-scope.mjs`、`lib/handoff-constants.mjs` 四个新模块都**没有直属单测文件**，
  于是"未被任何测试直接 import 的源模块"从 24 涨到 28。它们的行为确实被覆盖——两个 GC 调用方
  的测试经变异验证会随 `lib/shard-gc.mjs` 一起红，`lib/llm-call.mjs` 由 `tests/llm-timeout-budget.test.mjs`
  按文件路径盯着，`lib/quiet-scope.mjs` / `lib/handoff-constants.mjs` 经 `hook-shared.mjs` 的
  re-export 被 `tests/adopted-detection.test.mjs` 等覆盖——但这项指标数的是"直接 import"，
  所以这 4 个数字是真的，不该用"其实覆盖到了"糊过去。P2-4 的名单下轮应把它们算进去。
- **P2-8 仍为 Uncertain**：本轮 7 次全量跑（含 3 次带 coverage）**未复现**那次失败。P2-7 已落地，
  下次发生时名字会自动进报告行——这是把它从"无法核对"变成"可归因"的唯一改动，不是它已被解决。

# 下轮关注点

按收益/风险比排序，供 round 3 的 ②（只审不改）核对：

1. **P1-3 prettier 决策**——现在是本仓库最大的"假信号"：`npm run format:check` 恒红 525/531，
   而 `CLAUDE.md` 把它列为项目命令。需要用户在 (a) 一次性格式化 + 入 `.git-blame-ignore-revs` + 进 CI，
   与 (b) 删掉 `.prettierrc` 和两个 script 之间拍板。**它必须独占一个提交**，所以越晚做冲突越多。
2. **P2-9 标尺剥注释**——本轮新发现，且它污染的是 `docs/ARCHITECTURE.md §3` 这个刚刚入库、
   打算长期作为对照基线的文件。趁基线还年轻修掉，成本 ~3 LOC。
3. **P1-4 先量后改**——`doctor --metrics` 已经在记 per-hook 墙钟时间，报告要求的是 p50/p95 两个数字，
   不是重构。这一步本身零风险，且是解锁（或永久关闭）该条目的唯一前提。
4. **P2-1 拆 `handleStop` / `handleUserPrompt`**——前 4 项落地后热路径已经稳定，可以开始，
   但规则不变：一个 handler 一个提交，`doctor` 和 `initSchema` 不动。
5. **验证"复发"机制是否真的生效**——round 3 的第一步应当能直接读到本文件并逐条标 已解决/未解决/复发。
   如果读不到，说明 P1-1 只在本地生效（分支未合并），那才是真正要先解决的问题。

**下轮不要做**：`docs/audit/00-baseline.md` 里的数字不要与本文件的"本轮结束"列跨时间相减
（doctrine rule 2，两者相差 ~20 分钟且语料每次会话都在长）；需要 Δ 就同树重跑两臂。
